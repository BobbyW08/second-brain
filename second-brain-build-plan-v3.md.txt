Second Brain — Complete Build Plan V3
Coding Environment: Claude Code (terminal, planning + ticketing + review) + Cline/Qwen3-235B (VS Code, actual code writing) Project folder: C:\dev\second_brain Template folder: C:\dev\coding_templates Backup/Archive folder: C:\backup-archive


________________


Part 0 — Project Cleanup Instructions
Before writing a single line of new code, clean the C:\dev\second_brain folder.
Deliver the following instructions to Claude Code or Cline in a single session. The agent should follow each step sequentially and confirm completion before moving to the next.


________________




Step 0-A: Audit the existing folder
Read the full directory tree of C:\dev\second_brain recursively.


List all files by folder. Do not delete anything yet.


Output the full tree so I can review it.


________________




Step 0-B: Identify and extract template-worthy code
Before moving anything, scan for patterns worth saving. Ask the agent to check for:


* Any working Supabase client setup (typed client, auth helpers)
* Any working Google OAuth flow code
* Any .env or .env.example with the correct variable names
* Any working TanStack Query useQuery or useMutation hooks
* Any working RLS migration files
* Any utility functions (date helpers, debounce, formatters)


For each item found, the agent should:


1. Strip project-specific names and replace with [PROJECT_NAME], [TABLE_NAME], [YOUR_VALUE] placeholders
2. Save to the correct subfolder of C:\dev\coding_templates\


Template folder structure:


C:\dev\coding_templates\


  supabase\


    typed-client.ts          ← Supabase typed client setup


    auth-context.tsx         ← Auth provider + hook


    auth-hook.ts             ← useAuth() hook


    rls-policy-template.sql  ← RLS policy block patterns


    gen-types.sh             ← supabase gen types script


    profiles-trigger.sql     ← new user → profiles insert trigger


    invite-pattern.ts        ← shouldCreateUser:false + generateLink pattern


    google-token-storage.ts  ← provider_refresh_token storage on auth callback


  tanstack\


    protected-route.tsx      ← beforeLoad auth guard pattern


    query-hook.ts            ← useQuery + useMutation pattern


    optimistic-update.ts     ← optimistic update pattern


    server-function.ts       ← TanStack Start server function template


  ai\


    stream-route.ts          ← streamText + toDataStreamResponse route


    tone-prompt.ts           ← TONE_SYSTEM_PROMPT constant


    use-ai-context.ts        ← context builder hook


  components\


    auth-form.tsx            ← shadcn auth form (zod + RHF)


    autosave-hook.ts         ← debounced useMutation autosave


    empty-state.tsx          ← empty state with icon/title/cta


    inline-create-input.tsx  ← inline create field pattern


  google-calendar\


    calendar-server-fn.ts    ← googleapis read/write server function


    token-refresh.ts         ← offline access + refresh token pattern


  env\


    .env.example             ← all required env variable names


________________




Step 0-C: Move old code to archive
After saving templates, move everything from C:\dev\second_brain to C:\backup-archive\second_brain_old_[DATE].


Command for the agent:


Move all contents of C:\dev\second_brain to C:\backup-archive\second_brain_old_[today's date].


Do NOT delete the C:\dev\second_brain folder itself — just empty it.


Confirm each subfolder was moved before proceeding.


________________




Step 0-D: Verify the folder is clean
List the contents of C:\dev\second_brain.


It should be empty (or contain only a .git folder if version control was initialized).


Confirm it is ready for a fresh project scaffold.


________________




Step 0-E: Initialize the new project
Clone the repo https://github.com/domgaulton/tanstack-start-supabase-auth-protected-routes


into C:\dev\second_brain.


Rename the project in package.json to "second-brain".


Delete the cloned repo's .git folder and run git init to start fresh version control.


Run npm install to verify all dependencies resolve.


________________




Step 0-F: Create CLAUDE.md and Cline memory bank
Create C:\dev\second_brain\CLAUDE.md:


# Second Brain Project


## Stack


TanStack Start + Nitro | Supabase (PostgreSQL + RLS + Auth + Realtime) | shadcn/ui + Tailwind v4 | FullCalendar v6 | BlockNote + @blocknote/xl-ai | Zustand | Vercel AI SDK | assistant-ui | react-arborist | Sonner


## Hard Rules


- No Vite config. No separate api/ folder. All server logic in TanStack Start server functions.


- All Supabase calls use .throwOnError(). Never swallow errors silently.


- TONE: Never use "overdue", "late", "missed", "behind", "you should", "you need to", "failed", "incomplete" in any JSX, variable names, comments, or UI copy.


- Block size S/M/L = visual height only. Never a timer. Never evaluative.


- Every AI route MUST import TONE_SYSTEM_PROMPT from src/lib/aiConstants.ts. Never inline the system prompt.


- After completing each phase, save reusable patterns to C:\dev\coding_templates\ with placeholders.


- Use Zustand for client state. Use TanStack Query for server state. Never mix them.


## File Naming Conventions


- Components: PascalCase → src/components/[feature]/ComponentName.tsx


- Queries: camelCase → src/queries/featureName.ts


- Hooks: useCamelCase → src/hooks/useHookName.ts


- Server functions: camelCase → src/server/serverFnName.ts


- Types: PascalCase → src/types/TypeName.ts


- Constants: SCREAMING_SNAKE_CASE for values, camelCase for objects


## Template Saves


After each phase ticket is complete and accepted, extract reusable patterns to:


C:\dev\coding_templates\[subfolder]\template-name.ts


Strip all project-specific names. Add [PLACEHOLDER] comments everywhere a value changes per project.


Create Cline memory bank:


Create the following files in C:\dev\second_brain\memory-bank\:


  projectbrief.md   → Purpose: invite-only personal productivity app (Second Brain). Stack summary. Core tone rule.


  techContext.md    → Full stack list, key library versions, folder structure, env variable names.


  activeContext.md  → Current ticket being worked. Last completed ticket. Next 3 tickets queued.


  progress.md       → Checklist of all phases and tickets. Check off as completed.


  decisionLog.md    → Record why each major tool was chosen over alternatives.


________________


Part 1 — Architecture & Technology Stack
Confirmed Stack (No Vite — No Exceptions)
Layer
	Tool
	Version
	Reason
	Framework
	TanStack Start + Nitro
	latest
	SSR + server functions + Vercel deploy without separate API folder
	Routing
	TanStack Router
	latest
	File-based, type-safe, beforeLoad auth guard
	Database
	Supabase PostgreSQL
	—
	Free tier, RLS, Auth, Realtime, Storage
	Auth
	Supabase Auth
	—
	Google OAuth, invite-only, magic links, sessions
	UI components
	shadcn/ui
	latest
	Tailwind v4, copy-paste, fully owned
	Styling
	Tailwind CSS v4
	4.x
	Utility-first, responsive prefixes
	Calendar
	FullCalendar v6 React
	6.x
	timeGrid, drag-drop, external drag, nowIndicator
	Block editor
	BlockNote
	latest
	Slash commands, drag-drop blocks, custom content types
	AI editor
	@blocknote/xl-ai
	latest
	AI toolbar + /ai command + streaming — GPL-3
	Folder tree
	react-arborist
	latest
	Drag-drop, inline rename, keyboard nav — MIT
	AI chat UI
	assistant-ui
	latest
	Streaming thread UI, markdown, WAI-ARIA — MIT
	AI SDK
	Vercel AI SDK
	latest
	streamText, useChat, useCompletion, Anthropic adapter
	AI model
	Anthropic Claude
	API
	claude-3-5-sonnet for chat; claude-3-haiku for suggestions
	State (client)
	Zustand
	latest
	Persist, devtools, zero boilerplate
	State (server)
	TanStack Query
	latest
	Caching, invalidation, optimistic updates
	Forms
	React Hook Form + zod
	latest
	Type-safe validation, shadcn Form integration
	Toasts
	Sonner (shadcn)
	latest
	Non-blocking, undo button, promise handling
	Linting
	Biome
	latest
	ESLint + Prettier replacement, faster
	Testing
	Vitest
	latest
	Unit tests for pure functions (date logic, etc.)
	Deployment
	Vercel Hobby
	free
	Nitro adapter handles SSR + edge functions
	

________________


Part 2 — Database Schema
Core Tables
-- Users (managed by Supabase Auth)


-- profiles: extended user data


create table profiles (


  id uuid references auth.users on delete cascade primary key,


  email text not null,


  display_name text,


  avatar_url text,


  google_access_token text,        -- encrypted at rest


  google_refresh_token text,       -- encrypted at rest


  google_token_expiry timestamptz,


  google_calendar_id text,


  tone_preference text default 'encouraging',


  ai_usage_count integer default 0,


  created_at timestamptz default now(),


  updated_at timestamptz default now()


);


-- folders: nested hierarchy for pages


create table folders (


  id uuid default gen_random_uuid() primary key,


  user_id uuid references profiles(id) on delete cascade,


  parent_id uuid references folders(id) on delete cascade,


  name text not null,


  icon text,


  position integer default 0,


  created_at timestamptz default now()


);


-- pages: rich text content nodes


create table pages (


  id uuid default gen_random_uuid() primary key,


  user_id uuid references profiles(id) on delete cascade,


  folder_id uuid references folders(id) on delete set null,


  title text not null default 'Untitled',


  content jsonb,                   -- BlockNote JSON


  page_type text default 'page',  -- 'page' | 'journal'


  journal_date date,               -- for journal pages


  is_pinned boolean default false,


  created_at timestamptz default now(),


  updated_at timestamptz default now()


);


-- tasks: priority bucket items


create table tasks (


  id uuid default gen_random_uuid() primary key,


  user_id uuid references profiles(id) on delete cascade,


  title text not null,


  priority text not null,          -- 'urgent' | 'important' | 'someday'


  block_size text not null default 'M', -- 'S' | 'M' | 'L' (visual only)


  status text default 'active',    -- 'active' | 'completed_today' | 'archived'


  completed_at timestamptz,


  position integer default 0,


  notes jsonb,                     -- optional BlockNote JSON


  created_at timestamptz default now(),


  updated_at timestamptz default now()


);


-- calendar_blocks: scheduled time slots


create table calendar_blocks (


  id uuid default gen_random_uuid() primary key,


  user_id uuid references profiles(id) on delete cascade,


  task_id uuid references tasks(id) on delete set null,


  title text not null,


  start_time timestamptz not null,


  end_time timestamptz not null,


  block_type text default 'task',  -- 'task' | 'event' | 'focus' | 'break'


  google_event_id text,


  color text,


  is_synced boolean default false,


  created_at timestamptz default now(),


  updated_at timestamptz default now()


);


-- tables_schema: dynamic field tables


create table tables_schema (


  id uuid default gen_random_uuid() primary key,


  user_id uuid references profiles(id) on delete cascade,


  folder_id uuid references folders(id) on delete set null,


  name text not null,


  columns jsonb not null default '[]', -- array of { id, name, type, options }


  created_at timestamptz default now()


);


-- table_rows: rows for dynamic tables


create table table_rows (


  id uuid default gen_random_uuid() primary key,


  table_id uuid references tables_schema(id) on delete cascade,


  user_id uuid references profiles(id) on delete cascade,


  data jsonb not null default '{}',


  notes jsonb,                     -- optional BlockNote JSON for row detail


  position integer default 0,


  created_at timestamptz default now(),


  updated_at timestamptz default now()


);


-- links: bidirectional page/item linking


create table links (


  id uuid default gen_random_uuid() primary key,


  user_id uuid references profiles(id) on delete cascade,


  source_type text not null,       -- 'page' | 'task' | 'table_row'


  source_id uuid not null,


  target_type text not null,


  target_id uuid not null,


  created_at timestamptz default now()


);


-- invites: admin-issued invite tokens


create table invites (


  id uuid default gen_random_uuid() primary key,


  email text not null unique,


  token text not null unique,


  used boolean default false,


  invited_by uuid references profiles(id),


  created_at timestamptz default now(),


  expires_at timestamptz default (now() + interval '7 days')


);


-- ai_usage: per-user AI consumption tracking


create table ai_usage (


  id uuid default gen_random_uuid() primary key,


  user_id uuid references profiles(id) on delete cascade,


  feature text not null,           -- 'chat' | 'writing' | 'scheduling' | 'journal'


  tokens_used integer,


  created_at timestamptz default now()


);


RLS Policies (apply to all tables)
-- Enable RLS on all tables


alter table profiles enable row level security;


alter table folders enable row level security;


alter table pages enable row level security;


alter table tasks enable row level security;


alter table calendar_blocks enable row level security;


alter table tables_schema enable row level security;


alter table table_rows enable row level security;


alter table links enable row level security;


alter table ai_usage enable row level security;


-- Standard pattern (repeat for each table):


create policy "Users can only see their own [table]"


  on [table] for all


  using (auth.uid() = user_id);


________________


Part 3 — Phase Plan
Phase 0: Project Setup (1 session)
Ticket 0-A: Clone starter repo and initialize project


* Clone domgaulton/tanstack-start-supabase-auth-protected-routes
* Rename project in package.json to second-brain
* Reset git history: delete .git, run git init
* Install all dependencies: npm install
* Verify dev server runs: npm run dev


Ticket 0-B: Configure Supabase project


* Create new Supabase project
* Enable Google OAuth in Supabase Auth dashboard
* Run all database migrations from Part 2 schema above
* Enable RLS on all tables
* Run supabase gen types typescript --project-id [id] > src/types/database.types.ts
* Create .env.local with all required variables (see template)


Ticket 0-C: Configure Google OAuth scopes


* In Supabase dashboard → Auth → Providers → Google: add scope https://www.googleapis.com/auth/calendar
* In signInWithOAuth call: add options: { queryParams: { access_type: 'offline', prompt: 'consent' } }
* In auth callback: extract provider_refresh_token from session and save to profiles table
* Test full Google OAuth flow


Ticket 0-D: Create CLAUDE.md and memory bank


* Create CLAUDE.md at project root using template in Part 0 above
* Create memory-bank/ folder with all 5 files
* Test: open Cline in VS Code, ask it to "read memory bank" — verify it reads all files


Acceptance criteria: Dev server runs. Supabase connection works. Google OAuth login completes. profiles row created. provider_refresh_token stored.


TEMPLATE SAVE (0-B): Extract typed Supabase client, auth callback with token storage, gen-types.sh script, and RLS policy block to C:\dev\coding_templates\supabase\


________________




Here's the updated Phase 1 section to drop in — replace everything from ### Phase 1: Auth, Invite System & App Shell (2–3 sessions) through the end of that phase (stopping before ### Phase 2):

Phase 1: Auth, Invite System & App Shell (2–3 sessions)

Ticket 1-A: Invite-Only Signup Gate
Read memory-bank/activeContext.md, memory-bank/techContext.md, and CLAUDE.md before 
doing anything else. Follow all Hard Rules in CLAUDE.md throughout this ticket.

---

TICKET 1-A: Invite-Only Signup Gate
Project folder: C:\dev\second_brain
Current phase: Phase 1 — Auth, Invite System & App Shell

---

CONTEXT

The app is invite-only. Nobody can create an account unless an admin has generated an 
invite for their email address. This is enforced at the Supabase Auth level — not just 
a UI gate. The starter repo is already cloned, running, and all dependencies from this 
ticket have already been installed by Claude Code. Do not run any installs. Go straight 
to writing code.

All of the following are already installed and ready to use:
- @supabase/supabase-js
- @tanstack/react-query
- The TanStack Start createServerFn API is available via the framework

---

SUB-TASKS

1. LOCK DOWN OPEN SIGNUPS

Find every place in the codebase where signInWithOtp or signInWithOAuth is called.
Add shouldCreateUser: false to each call's options object.

For signInWithOAuth (Google), the full options block must look like this:

  await supabase.auth.signInWithOAuth({
    provider: 'google',
    options: {
      scopes: 'https://www.googleapis.com/auth/calendar',
      queryParams: {
        access_type: 'offline',
        prompt: 'consent',
      },
      shouldCreateUser: false,
    },
  })

This means: if the email doesn't already exist as a Supabase Auth user, the login 
attempt is rejected. Non-invited users cannot create accounts even if they reach the 
login page.

2. BUILD THE ADMIN INVITE PAGE

Create a new route at /admin/invite using TanStack Router file-based routing.
File path: src/routes/admin/invite.tsx

Protection: At the top of the route, add a beforeLoad guard that checks if the 
logged-in user's email matches the ADMIN_EMAIL environment variable 
(import.meta.env.VITE_ADMIN_EMAIL). If it doesn't match, redirect to '/'.

The page UI is a single form:
- Email input (text, required, type="email")
- "Send Invite" button
- Do NOT use HTML <form> tags. Use a controlled input with useState and an onClick 
  handler on the button.
- Show a success message inline after invite sends (e.g. "Invite sent to 
  user@example.com")
- Show an error message inline if it fails
- After a successful invite, also display the action link on the page as a fallback: 
  "Or copy this link manually: [link]" — useful for local testing where email 
  delivery may not work

3. CREATE THE SERVER FUNCTION FOR GENERATING INVITE LINKS

File path: src/server/inviteUser.ts

Use TanStack Start's createServerFn. The function takes { email: string } as input.

Inside the function:
- Create a Supabase admin client using SUPABASE_SERVICE_ROLE_KEY (never the anon 
  key — admin operations require the service role key)
- Call supabase.auth.admin.generateLink({ type: 'invite', email })
- Insert a record into the invites table: 
  { email, token: data.properties.hashed_token, invited_by: currentUserId }
- Return the action_link from data.properties.action_link

The admin client is created like this:
  import { createClient } from '@supabase/supabase-js'
  const adminClient = createClient(
    import.meta.env.VITE_SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!
  )

After generateLink succeeds, insert to the invites table:
  await adminClient
    .from('invites')
    .insert({
      email: email,
      token: data.properties.hashed_token,
      invited_by: currentUser.id,
    })
    .throwOnError()

All Supabase calls end with .throwOnError(). No silent error swallowing.

---

FILES TO CREATE OR MODIFY

- src/routes/admin/invite.tsx — new file
- src/server/inviteUser.ts — new file
- Whichever file(s) contain the existing signInWithOtp / signInWithOAuth calls — 
  modify to add shouldCreateUser: false

---

HARD RULES REMINDER

- All server logic goes in src/server/ using createServerFn. No api/ folder. No Vite.
- All Supabase calls end with .throwOnError()
- No HTML <form> tags — use controlled inputs and onClick handlers
- SUPABASE_SERVICE_ROLE_KEY is used only in server functions, never on the client
- No tone violations: avoid "failed", "missed", "you need to" in any UI copy

---

ACCEPTANCE CRITERIA

- /admin/invite is only accessible to the admin email — all other logged-in users 
  are redirected to '/'
- Submitting an email generates a Supabase invite link and writes a record to the 
  invites table
- The generated action link is displayed on the page after a successful invite
- A non-invited email cannot create an account via Google OAuth or magic link
- No console errors on any of the above flows

---

WHEN DONE

1. Run npm run dev and confirm no console errors
2. Update memory-bank/activeContext.md: mark 1-A complete, set 1-B as current ticket
3. Update memory-bank/progress.md: check off Ticket 1-A
4. Save the invite pattern to C:\dev\coding_templates\supabase\invite-pattern.ts — 
   strip all project-specific values, replace with [PROJECT_NAME], [ADMIN_EMAIL], 
   [TABLE_NAME] placeholders

Ticket 1-B: App Shell Layout
Read memory-bank/activeContext.md, memory-bank/techContext.md, and CLAUDE.md before 
doing anything else. Follow all Hard Rules in CLAUDE.md throughout this ticket.

---

TICKET 1-B: App Shell Layout
Project folder: C:\dev\second_brain
Current phase: Phase 1 — Auth, Invite System & App Shell

---

CONTEXT

This ticket builds the persistent layout that wraps every authenticated page in the 
app. It is the visual skeleton — sidebar, top bar, and main content area — that all 
future features will live inside. The folder tree in the left panel is a placeholder 
only; the real tree is built in Phase 4. This ticket must produce a complete, 
functional shell that looks intentional, not like scaffolding.

All of the following shadcn components and packages have already been installed by 
Claude Code. Do not run any installs. Go straight to writing code.

Already installed and ready to import:
- shadcn Sidebar → SidebarProvider, Sidebar, SidebarContent, SidebarHeader, 
  SidebarFooter, SidebarTrigger, SidebarMenu, SidebarMenuItem
- shadcn Drawer → Drawer, DrawerContent, DrawerTrigger
- shadcn Avatar → Avatar, AvatarImage, AvatarFallback
- shadcn Separator
- shadcn Tooltip → Tooltip, TooltipContent, TooltipTrigger, TooltipProvider
- shadcn DropdownMenu → DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, 
  DropdownMenuItem
- shadcn ThemeProvider + useTheme (ModeToggle pattern)
- lucide-react (icons)
- @tanstack/react-router → Outlet, Link, useNavigate

---

SUB-TASKS

1. BUILD AppLayout.tsx

File path: src/components/layout/AppLayout.tsx

This component wraps all authenticated routes. Structure:

  <SidebarProvider>
    <div className="flex h-screen w-full overflow-hidden">
      <AppSidebar />
      <div className="flex flex-col flex-1 overflow-hidden">
        <TopBar />
        <main className="flex-1 overflow-y-auto p-4">
          <Outlet />
        </main>
      </div>
    </div>
  </SidebarProvider>

Import Outlet from '@tanstack/react-router'.

2. BUILD AppSidebar.tsx

File path: src/components/layout/AppSidebar.tsx

Use shadcn's Sidebar, SidebarContent, SidebarHeader, SidebarFooter, SidebarTrigger.

Structure:
- SidebarHeader: App name/logo text "Second Brain" + SidebarTrigger (collapse button)
- SidebarContent:
  - Static nav items using SidebarMenu and SidebarMenuItem, each a Link:
    - 📅 Calendar → /calendar
    - 📝 Pages → /pages
    - 📓 Journal → /journal
    - 🗃️ Tables → /tables
  - Below nav: a section labeled "Pages" with 2–3 static dummy folder/page items 
    showing the tree shape. No data fetching. Clearly comment: 
    // TODO: Phase 4 — replaced by react-arborist FolderTree
- SidebarFooter: User avatar (from Supabase auth session) + display name + settings 
  gear icon linking to /settings

The sidebar must be collapsible. SidebarTrigger handles this automatically via 
SidebarProvider context — no custom collapse state needed.

3. BUILD TopBar.tsx

File path: src/components/layout/TopBar.tsx

A horizontal bar at the top of the main content area (not the sidebar). Height: h-12.

Contents left to right:
- Left: Static breadcrumb text "Second Brain" — will be dynamic in a later phase
- Center: empty
- Right (left to right):
  - Search icon button (magnifying glass) labeled "⌘K". onClick does nothing yet.
    Add comment: // TODO: Phase 6 — open CommandDialog on click
  - Bell icon button. onClick does nothing yet.
    Add comment: // TODO: Phase 8 — notifications
  - Theme toggle (sun/moon icon) using useTheme() — toggles light/dark/system
  - User avatar with DropdownMenu:
    - Shows avatar_url from Supabase session, or initials fallback from display_name
    - Dropdown items: "Settings" (Link to /settings), "Sign out" (calls 
      supabase.auth.signOut())

All icon buttons must be at least 44×44px tap targets.

4. DARK MODE

Wrap the root app entry point (not just AppLayout) with ThemeProvider:

  <ThemeProvider defaultTheme="system" storageKey="second-brain-theme">
    ...
  </ThemeProvider>

The ModeToggle in TopBar reads and sets theme via useTheme(). Theme persists to 
localStorage automatically via the storageKey prop.

5. MOBILE: SIDEBAR → BOTTOM DRAWER

On screens smaller than md (768px):
- Hide AppSidebar: className="hidden md:flex" on the sidebar wrapper
- Add a floating ☰ button: className="fixed bottom-4 right-4 md:hidden z-50"
  - Minimum size: 44×44px
  - Opens a shadcn Drawer from the bottom
- Inside the Drawer: render the same nav items as AppSidebar
- Drawer closes automatically after a nav item is tapped (call setOpen(false) in 
  each item's onClick)

6. WIRE AppLayout INTO THE AUTHENTICATED ROUTE TREE

Find the authenticated route layout file in the TanStack Router file structure 
(likely src/routes/_authenticated.tsx — check what the starter repo uses).

If the file exists, modify it to render AppLayout as its component:

  export const Route = createFileRoute('/_authenticated')({
    beforeLoad: async ({ context }) => {
      if (!context.user) throw redirect({ to: '/login' })
    },
    component: AppLayout,
  })

If the starter repo uses a different pattern, adapt accordingly — do not break 
the existing auth guard, only add AppLayout as the component.

---

FILES TO CREATE OR MODIFY

- src/components/layout/AppLayout.tsx — new file
- src/components/layout/AppSidebar.tsx — new file
- src/components/layout/TopBar.tsx — new file
- src/routes/_authenticated.tsx (or equivalent) — modify to use AppLayout
- Root app entry point — add ThemeProvider wrapper

---

HARD RULES REMINDER

- No api/ folder. No Vite config. No installs — everything is already set up.
- All Supabase calls (signOut, profile reads) end with .throwOnError()
- No HTML <form> tags
- No tone violations in any text strings, variable names, comments, or class names
- All tap targets minimum 44×44px

---

ACCEPTANCE CRITERIA

- App loads after login and shows sidebar + top bar + main content area
- Sidebar collapses and expands via SidebarTrigger
- Dark mode toggle works and persists after page refresh
- On a 375px viewport, the sidebar is hidden and the bottom drawer trigger appears 
  and opens correctly
- Tapping a nav item in the mobile drawer closes the drawer
- Sign out from the top bar avatar dropdown works
- No console errors at 375px and 1280px viewport widths

---

WHEN DONE

1. Run npm run dev and confirm no console errors at both 375px and 1280px
2. Update memory-bank/activeContext.md: mark 1-B complete, set 1-C as current ticket
3. Update memory-bank/progress.md: check off Ticket 1-B
4. Save the app shell pattern to C:\dev\coding_templates\components\app-shell.tsx — 
   strip all project-specific copy, replace with [APP_NAME], [NAV_ITEMS], 
   [PLACEHOLDER] comments

Ticket 1-C: User Settings Page
Read memory-bank/activeContext.md, memory-bank/techContext.md, and CLAUDE.md before 
doing anything else. Follow all Hard Rules in CLAUDE.md throughout this ticket.

---

TICKET 1-C: User Settings Page
Project folder: C:\dev\second_brain
Current phase: Phase 1 — Auth, Invite System & App Shell

---

CONTEXT

The settings page is where users manage their profile, appearance, and connected 
services. It reads from and writes to the profiles table in Supabase. The app shell 
from Ticket 1-B is already in place — this page renders inside it automatically.

All packages and shadcn components needed for this ticket have already been installed 
by Claude Code. Do not run any installs. Go straight to writing code.

Already installed and ready to import:
- react-hook-form
- zod + @hookform/resolvers/zod
- shadcn Form → Form, FormControl, FormField, FormItem, FormLabel, FormMessage
- shadcn Input
- shadcn Label
- shadcn Button
- shadcn Separator
- shadcn Badge
- shadcn Avatar → Avatar, AvatarImage, AvatarFallback
- shadcn Sonner → toast (from 'sonner'), Toaster component
- @tanstack/react-query → useQuery, useMutation, useQueryClient
- @supabase/supabase-js (Supabase client already configured in the project)

Toaster: confirm <Toaster /> is present in the root layout from Ticket 1-B. If it 
is not there yet, add it now to the root layout file.

---

SUB-TASKS

1. CREATE THE SETTINGS ROUTE

File path: src/routes/settings.tsx

This route sits inside the authenticated layout and is covered by the _authenticated 
beforeLoad guard automatically. No additional auth guard needed on this file.

The route renders <SettingsPage />.

2. BUILD THE DATA LAYER

File path: src/queries/profile.ts

Create two TanStack Query hooks. All Supabase calls end with .throwOnError().

useProfile(userId: string):
  export function useProfile(userId: string) {
    return useQuery({
      queryKey: ['profile', userId],
      queryFn: async () => {
        const { data } = await supabase
          .from('profiles')
          .select('*')
          .eq('id', userId)
          .single()
          .throwOnError()
        return data
      },
    })
  }

useUpdateProfile():
  export function useUpdateProfile() {
    const queryClient = useQueryClient()
    return useMutation({
      mutationFn: async ({ 
        userId, 
        updates 
      }: { 
        userId: string
        updates: Partial<Profile> 
      }) => {
        await supabase
          .from('profiles')
          .update(updates)
          .eq('id', userId)
          .throwOnError()
      },
      onSuccess: (_data, { userId }) => {
        queryClient.invalidateQueries({ queryKey: ['profile', userId] })
      },
    })
  }

Profile type comes from src/types/database.types.ts (generated from Supabase schema 
in Phase 0). Use the correct generated type — do not define a manual Profile type.

3. BUILD THE SETTINGS PAGE COMPONENT

File path: src/components/settings/SettingsPage.tsx

Container: max-w-2xl mx-auto px-4 py-8

Page title: "Settings" as an h1 at the top.

Four sections separated by shadcn Separators:

---

SECTION 1 — PROFILE

Use React Hook Form + zod for this section.

Schema:
  const profileSchema = z.object({
    display_name: z.string().min(1, 'Name is required').max(100),
    avatar_url: z.string().url('Must be a valid URL').or(z.literal('')),
  })

Wire defaultValues from useProfile(). Use useEffect to reset the form when profile 
data loads:
  useEffect(() => {
    if (profile) {
      form.reset({
        display_name: profile.display_name ?? '',
        avatar_url: profile.avatar_url ?? '',
      })
    }
  }, [profile])

Fields:
- Display name — FormField with Input
- Avatar URL — FormField with Input
  - Below the input: live avatar preview using shadcn Avatar
    - If avatar_url is a valid URL, show AvatarImage
    - If empty, show AvatarFallback with initials derived from display_name

Save button: "Save profile" — triggers form submit → calls useUpdateProfile()

On success: toast.success('Profile updated')
On error: toast.error('Could not save profile. Try again.')

---

SECTION 2 — APPEARANCE

Label "Theme" on the left, ModeToggle on the right (reuse the same toggle built in 
Ticket 1-B using useTheme()). No save button — theme changes apply instantly.

---

SECTION 3 — AI PREFERENCES (PLACEHOLDER)

Heading: "AI Preferences"
Description: "Personalization options for your AI assistant will appear here."
Badge: "Coming soon" in muted/secondary styling.
No interactive elements. Add comment: // TODO: Phase 7 — AI preferences

---

SECTION 4 — CONNECTED SERVICES

Read google_calendar_id and google_refresh_token from the profiles row.

If both are present:
- Show a green "Connected" badge
- Show the google_calendar_id value in small muted text
- Show a "Disconnect" button

If either is missing:
- Show a "Connect Google Calendar" button

"Connect Google Calendar" button:
  await supabase.auth.signInWithOAuth({
    provider: 'google',
    options: {
      scopes: 'https://www.googleapis.com/auth/calendar',
      queryParams: {
        access_type: 'offline',
        prompt: 'consent',
      },
      shouldCreateUser: false,
    },
  })

"Disconnect" button — calls useUpdateProfile() with:
  {
    google_access_token: null,
    google_refresh_token: null,
    google_token_expiry: null,
    google_calendar_id: null,
  }

On disconnect success: toast.success('Google Calendar disconnected')
On disconnect error: toast.error('Could not disconnect. Try again.')

Does NOT revoke the OAuth token on Google's side — out of scope for MVP.

---

FILES TO CREATE OR MODIFY

- src/routes/settings.tsx — new file
- src/components/settings/SettingsPage.tsx — new file
- src/queries/profile.ts — new file
- Root layout file — confirm <Toaster /> is present; add it if missing

---

HARD RULES REMINDER

- All Supabase calls end with .throwOnError()
- Do not write raw <form> tags with action attributes. Use shadcn Form (which wraps 
  React Hook Form's handleSubmit) — that is the correct pattern.
- No tone violations anywhere — the placeholder copy above is already tone-safe, 
  use it exactly as written
- SUPABASE_SERVICE_ROLE_KEY is never used here — all profile reads and writes use 
  the anon client with RLS
- All data fetching and mutation goes through TanStack Query hooks in 
  src/queries/profile.ts — no direct Supabase calls inside the component

---

ACCEPTANCE CRITERIA

- /settings renders inside the app shell
- Profile section loads display name and avatar from Supabase on mount
- Saving profile updates the profiles row and shows a success toast
- Refreshing the page after a save shows the updated values
- Theme toggle works and persists across refresh (from Ticket 1-B, just verify it 
  still works here)
- Google Calendar section shows "Connected" + calendar ID when tokens are present
- Google Calendar section shows "Connect" button when tokens are absent
- Disconnect button clears all four token fields and shows a success toast
- No console errors

---

WHEN DONE

1. Run npm run dev and confirm no console errors
2. Manual test: change display name → save → refresh → confirm new name persists
3. Update memory-bank/activeContext.md: mark 1-C complete, set Ticket 2-A as next
4. Update memory-bank/progress.md: check off Ticket 1-C and mark Phase 1 complete
5. No template save required for this ticket
Acceptance criteria (Phase 1): App loads after login. Sidebar renders. Non-invited users blocked. Settings page saves changes.
TEMPLATE SAVE (1-A): Extract invite-only pattern to C:\dev\coding_templates\supabase\invite-pattern.ts
TEMPLATE SAVE (1-B): Extract app shell layout to C:\dev\coding_templates\components\app-shell.tsx
________________


# Phase 2: Priority Bucket (Tasks) — Tickets 2-A, 2-B, 2-C

---

## Ticket 2-A: Task Data Layer

Read memory-bank/activeContext.md, memory-bank/techContext.md, and CLAUDE.md before
doing anything else. Follow all Hard Rules in CLAUDE.md throughout this ticket.

---

### TICKET 2-A: Task Data Layer
Project folder: C:\dev\second_brain
Current phase: Phase 2 — Priority Bucket (Tasks)

---

### VERIFICATION — READ BEFORE WRITING ANY CODE

Do not run `npm install`. All packages are pre-installed.
The `.npmrc` file already contains `legacy-peer-deps=true` — do not attempt to install or reinstall any dependency. Running `npm install` without this flag active will trigger peer dependency conflicts with `@blocknote/mantine`. The file is already there; trust it.

You are already in the project root: `C:\dev\second_brain`. Do not run `cd` commands. Running `cd second_brain` from this directory will fail because there is no nested folder by that name.

To verify your work, run these two commands only:

```
npm run typecheck   ← must exit with 0 errors
npm run lint        ← must exit with 0 errors
```

Do **NOT** run `npm run dev`. Claude Code will review and fix anything remaining.

If either command reports a missing script, you are likely in the wrong directory or package.json has not been re-read. Confirm you are in `C:\dev\second_brain`, open `package.json`, and check the `"scripts"` block before retrying. The `"typecheck"` script is defined as `"tsc --noEmit"` — if it appears missing, re-read the file rather than running installs.

---

### CONTEXT

This ticket builds the data layer that all task UI in Phase 2 depends on. No UI is
built here — only queries, mutations, and the Zustand store. Get this right before
any component work begins. All dependencies have already been installed by Claude Code.
Do not run any installs. Go straight to writing code.

Already installed and ready to import:
- `@supabase/supabase-js` (client already configured in the project)
- `@tanstack/react-query` → `useQuery`, `useMutation`, `useQueryClient`
- `zustand`

---

### SUB-TASKS

#### 1. BUILD THE TASK QUERY HOOKS

File path: `src/queries/tasks.ts`

All Supabase calls end with `.throwOnError()`. All hooks accept `userId` as a parameter
or read it from the auth context — do not hardcode any user ID.

**useTasksByPriority(userId: string)**
Returns all tasks for the user where `status = 'active'`, ordered by `position`.
Query key: `['tasks', userId]`

```ts
export function useTasksByPriority(userId: string) {
  return useQuery({
    queryKey: ['tasks', userId],
    queryFn: async () => {
      const { data } = await supabase
        .from('tasks')
        .select('*')
        .eq('user_id', userId)
        .eq('status', 'active')
        .order('position')
        .throwOnError()
      return data
    },
  })
}
```

**useCreateTask()**
Inserts a new task. Input: `{ user_id, title, priority, block_size?, position? }`
On success: invalidate `['tasks', userId]`
Include an optimistic update that appends the new task to the cache immediately.

**useMoveTask()**
Updates a task's `priority` and `position`. Input: `{ taskId, priority, position }`
On success: invalidate `['tasks', userId]`
Include an optimistic update.

**useCompleteTask()**
Updates `status` to `'completed_today'` and sets `completed_at` to `now()`.
Input: `taskId` (string)
On success: invalidate `['tasks', userId]`

Full optimistic update pattern (use this exact shape for all mutations):

```ts
onMutate: async (taskId) => {
  await queryClient.cancelQueries({ queryKey: ['tasks', userId] })
  const previous = queryClient.getQueryData(['tasks', userId])
  queryClient.setQueryData(['tasks', userId], (old: Task[]) =>
    old.map(t => t.id === taskId
      ? { ...t, status: 'completed_today', completed_at: new Date().toISOString() }
      : t
    )
  )
  return { previous }
},
onError: (_err, _taskId, context) => {
  queryClient.setQueryData(['tasks', userId], context?.previous)
},
onSettled: () => queryClient.invalidateQueries({ queryKey: ['tasks', userId] }),
```

**useArchiveTask()**
Updates `status` to `'archived'`. Input: `taskId` (string)
Same optimistic update pattern as `useCompleteTask`.

**useUpdateTask()**
General-purpose update. Input: `{ taskId: string, updates: Partial<Task> }`
On success: invalidate `['tasks', userId]`
Used for title edits and `block_size` changes.

`Task` type comes from `src/types/database.types.ts` (generated from Supabase schema).
Use the generated type — do not define a manual `Task` type.

---

#### 2. BUILD THE TASK ZUSTAND STORE

File path: `src/stores/taskStore.ts`

This store holds UI-only state that does not belong in TanStack Query.

```ts
import { create } from 'zustand'

interface TaskStore {
  selectedTaskId: string | null
  setSelectedTaskId: (id: string | null) => void
  dragState: {
    isDragging: boolean
    taskId: string | null
  }
  setDragState: (state: { isDragging: boolean; taskId: string | null }) => void
}

export const useTaskStore = create<TaskStore>((set) => ({
  selectedTaskId: null,
  setSelectedTaskId: (id) => set({ selectedTaskId: id }),
  dragState: { isDragging: false, taskId: null },
  setDragState: (dragState) => set({ dragState }),
}))
```

---

#### 3. DEFINE BLOCK SIZE CONSTANTS

File path: `src/lib/taskConstants.ts`

```ts
export const BLOCK_SIZE_DURATIONS: Record<'S' | 'M' | 'L', string> = {
  S: '00:30',  // 30 minutes
  M: '01:00',  // 60 minutes
  L: '01:30',  // 90 minutes
}

export const PRIORITY_LABELS: Record<string, string> = {
  urgent: 'Urgent',
  important: 'Important',
  someday: 'Someday',
}

export const PRIORITY_ORDER = ['urgent', 'important', 'someday'] as const
```

These constants are imported by Phase 3 (FullCalendar drag) — define them exactly
as shown. `BLOCK_SIZE_DURATIONS` values are FullCalendar duration strings.

---

### FILES TO CREATE

- `src/queries/tasks.ts` — new file
- `src/stores/taskStore.ts` — new file
- `src/lib/taskConstants.ts` — new file

---

### HARD RULES REMINDER

- All Supabase calls end with `.throwOnError()`
- Use Zustand for UI/client state only. Use TanStack Query for all server state.
- Never mix them — no Supabase calls inside the Zustand store
- No tone violations in variable names, comments, or string values:
  avoid "overdue", "late", "missed", "behind", "failed", "incomplete"
- Task status values in the DB are `'active'`, `'completed_today'`, `'archived'` —
  these are the only valid values, never invent others

---

### ACCEPTANCE CRITERIA

- All six hooks are exported from `src/queries/tasks.ts` and TypeScript compiles
  with no errors
- `useCompleteTask` includes a full optimistic update with rollback on error
- `useCreateTask` includes a full optimistic update
- Zustand store is typed correctly and exported
- `BLOCK_SIZE_DURATIONS` values match exactly: `S='00:30'`, `M='01:00'`, `L='01:30'`
- `npm run typecheck` exits with 0 errors
- `npm run lint` exits with 0 errors

---

### WHEN DONE

1. Run `npm run typecheck` — must exit with 0 errors
2. Run `npm run lint` — must exit with 0 errors
3. Update `memory-bank/activeContext.md`: mark 2-A complete, set 2-B as current ticket
4. Update `memory-bank/progress.md`: check off Ticket 2-A
5. Save the TanStack Query hook pattern with optimistic update to
   `C:\dev\coding_templates\tanstack\query-hook.ts` — strip all project-specific
   names, replace with `[TABLE_NAME]`, `[QUERY_KEY]`, `[USER_ID]` placeholders

---
---

## Ticket 2-B: Priority Bucket UI

Read memory-bank/activeContext.md, memory-bank/techContext.md, and CLAUDE.md before
doing anything else. Follow all Hard Rules in CLAUDE.md throughout this ticket.

---

### TICKET 2-B: Priority Bucket UI
Project folder: C:\dev\second_brain
Current phase: Phase 2 — Priority Bucket (Tasks)

---

### VERIFICATION — READ BEFORE WRITING ANY CODE

Do not run `npm install`. All packages are pre-installed.
The `.npmrc` file already contains `legacy-peer-deps=true` — do not attempt to install or reinstall any dependency.

You are already in the project root: `C:\dev\second_brain`. Do not run `cd` commands.

To verify your work, run these two commands only:

```
npm run typecheck   ← must exit with 0 errors
npm run lint        ← must exit with 0 errors
```

Do **NOT** run `npm run dev`. Claude Code will review and fix anything remaining.

If either command reports a missing script, confirm you are in `C:\dev\second_brain` and re-read `package.json` before retrying. The `"typecheck"` script runs `tsc --noEmit` — if it appears missing, the issue is directory context or a stale shell, not a missing package.

---

### CONTEXT

This ticket builds the Priority Bucket — the three-section task list (Urgent,
Important, Someday) that lives in the left panel of the app. The data layer from
Ticket 2-A is already in place. This ticket is UI only — wire to the existing hooks,
do not rewrite them.

The `TaskPill` component MUST include specific data attributes required by Phase 3's
FullCalendar Draggable. Add them now exactly as specified even though drag is not
wired until Phase 3.

All dependencies have already been installed by Claude Code. Do not run any installs.

Already installed and ready to import:
- shadcn `Button`, `Input`, `Badge`, `Collapsible` →
  `CollapsibleTrigger`, `CollapsibleContent`
- `lucide-react` (icons)
- `@tanstack/react-query` (hooks from `src/queries/tasks.ts`)
- `zustand` (store from `src/stores/taskStore.ts`)
- `src/lib/taskConstants.ts` (`PRIORITY_LABELS`, `PRIORITY_ORDER`, `BLOCK_SIZE_DURATIONS`)

---

### SUB-TASKS

#### 1. BUILD TaskPill.tsx

File path: `src/components/tasks/TaskPill.tsx`

This is the individual task card. Props:

```ts
interface TaskPillProps {
  task: Task  // from database.types.ts
  onComplete: (taskId: string) => void
}
```

Structure:

```tsx
<div
  className="task-pill ..."
  data-task-id={task.id}
  data-title={task.title}
  data-block-size={task.block_size}
  data-priority={task.priority}
>
  {/* Complete circle button — left side */}
  <button
    onClick={() => onComplete(task.id)}
    className="..."  {/* minimum 44x44px tap target */}
    aria-label="Mark complete"
  >
    <Circle className="..." />  {/* lucide-react */}
  </button>

  {/* Task title — center */}
  <span className="flex-1 text-sm">{task.title}</span>

  {/* Block size badge — right side */}
  <Badge variant="outline" className="text-xs">{task.block_size}</Badge>
</div>
```

The data attributes on the outer div are REQUIRED for Phase 3. Do not remove or
rename them. The className `"task-pill"` on the outer div is also required —
FullCalendar's Draggable uses it as the `itemSelector`.

The complete button must be at least 44×44px. Use padding to achieve this if needed.

Do not add any visual indicator that implies the task is urgent in a negative sense.
The priority label is informational only.

---

#### 2. BUILD PriorityBucket.tsx

File path: `src/components/tasks/PriorityBucket.tsx`

This is the full three-section task panel. It reads from `useTasksByPriority()` and
renders one section per priority level.

Props: `{ userId: string }`

Structure:
- A ref on the outer container div: `const bucketRef = useRef<HTMLDivElement>(null)`
  This ref is required for Phase 3's FullCalendar Draggable init. Add it now.
  Add a comment: `// bucketRef used by Phase 3 — FullCalendar Draggable init`

- For each priority in `PRIORITY_ORDER` (`['urgent', 'important', 'someday']`):

```tsx
<section key={priority}>
  <div className="flex items-center justify-between px-2 py-1">
    <h2 className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">
      {PRIORITY_LABELS[priority]}
    </h2>
    <button onClick={() => setCreating(priority)} aria-label="Add task">
      <Plus className="h-4 w-4" />  {/* lucide-react */}
    </button>
  </div>

  {/* Inline create input — appears when creating === priority */}
  {creating === priority && (
    <InlineTaskInput
      onSave={(title) => handleCreate(title, priority)}
      onCancel={() => setCreating(null)}
    />
  )}

  {/* Task pills */}
  {tasksForPriority.map(task => (
    <TaskPill
      key={task.id}
      task={task}
      onComplete={handleComplete}
    />
  ))}
</section>
```

State: `const [creating, setCreating] = useState<string | null>(null)`

Handlers:
- `handleCreate(title, priority)`: calls `useCreateTask()` with `{ user_id, title, priority, block_size: 'M', position: tasksForPriority.length }`
- `handleComplete(taskId)`: calls `useCompleteTask()`

After `handleCreate` resolves: set `creating` back to `null`.

---

#### 3. BUILD InlineTaskInput.tsx

File path: `src/components/tasks/InlineTaskInput.tsx`

A minimal controlled input that appears inline in the bucket when "+" is clicked.

Props:

```ts
interface InlineTaskInputProps {
  onSave: (title: string) => void
  onCancel: () => void
}
```

Behavior:
- Auto-focuses on mount (`useEffect` with `inputRef.current?.focus()`)
- `Enter` key → calls `onSave(value)` if value is not empty, then clears input
- `Escape` key → calls `onCancel()`
- `onBlur` → calls `onCancel()` (dismiss if user clicks away without saving)
- Do NOT use HTML `<form>` tags. Use a controlled `<Input>` with `onKeyDown` handler.

---

#### 4. WIRE PriorityBucket INTO THE APP LAYOUT

The bucket lives in the left sidebar area. In the app shell from Ticket 1-B, the
sidebar currently has static placeholder content below the nav items.

Find the section in `AppSidebar.tsx` marked with the TODO comment from Ticket 1-B and
replace the static placeholder with `<PriorityBucket userId={currentUser.id} />`.

Get `currentUser` from the Supabase auth session — use whatever auth context pattern
the starter repo established.

---

#### 5. ADD A ROUTE FOR THE BUCKET VIEW

Create a placeholder route at `/tasks` if one does not already exist. This is the
default landing route after login for now. It can render a simple centered message:
`"Select a task to get started, or drag one to the calendar."`

Add a comment: `// TODO: Phase 3 — calendar view replaces this as the main content`

---

### FILES TO CREATE OR MODIFY

- `src/components/tasks/TaskPill.tsx` — new file
- `src/components/tasks/PriorityBucket.tsx` — new file
- `src/components/tasks/InlineTaskInput.tsx` — new file
- `src/components/layout/AppSidebar.tsx` — modify to render PriorityBucket
- `src/routes/tasks/index.tsx` — new file (placeholder route)

---

### HARD RULES REMINDER

- The outer div of `TaskPill` must have `className="task-pill"` and all four data
  attributes. These are load-bearing for Phase 3 — do not skip or rename them.
- All tap targets minimum 44×44px
- No HTML `<form>` tags — use controlled `Input` with `onKeyDown`
- No tone violations: do not use "overdue", "late", "missed", "behind", "failed",
  "incomplete" anywhere in copy, class names, variable names, or comments
- Block size S/M/L is visual only — never describe it as a time estimate in any
  UI copy or aria label

---

### ACCEPTANCE CRITERIA

- Three priority sections render (Urgent, Important, Someday)
- Clicking "+" in any section shows the inline input in that section only
- Pressing Enter with a title saves the task and closes the input
- Pressing Escape or clicking away closes the input without saving
- New tasks appear immediately (optimistic update from 2-A)
- Clicking the complete circle on a TaskPill calls `useCompleteTask()` and the task
  disappears from the active list immediately (optimistic update)
- `TaskPill` outer div has `className="task-pill"` and all four data attributes present
  in the DOM (verify in browser devtools)
- `bucketRef` is attached to the outer container of `PriorityBucket`
- `npm run typecheck` exits with 0 errors
- `npm run lint` exits with 0 errors

---

### WHEN DONE

1. Run `npm run typecheck` — must exit with 0 errors
2. Run `npm run lint` — must exit with 0 errors
3. Manually test: create a task in each bucket, complete one, verify it disappears
4. Open browser devtools → inspect a TaskPill → confirm `data-task-id`, `data-title`,
   `data-block-size`, `data-priority` are all present on the element
5. Update `memory-bank/activeContext.md`: mark 2-B complete, set 2-C as current ticket
6. Update `memory-bank/progress.md`: check off Ticket 2-B

---
---

## Ticket 2-C: Completed Today Section

Read memory-bank/activeContext.md, memory-bank/techContext.md, and CLAUDE.md before
doing anything else. Follow all Hard Rules in CLAUDE.md throughout this ticket.

---

### TICKET 2-C: Completed Today Section
Project folder: C:\dev\second_brain
Current phase: Phase 2 — Priority Bucket (Tasks)

---

### VERIFICATION — READ BEFORE WRITING ANY CODE

Do not run `npm install`. All packages are pre-installed.
The `.npmrc` file already contains `legacy-peer-deps=true` — do not attempt to install or reinstall any dependency.

You are already in the project root: `C:\dev\second_brain`. Do not run `cd` commands.

To verify your work, run these two commands only:

```
npm run typecheck   ← must exit with 0 errors
npm run lint        ← must exit with 0 errors
```

Do **NOT** run `npm run dev`. Claude Code will review and fix anything remaining.

If either command reports a missing script, confirm you are in `C:\dev\second_brain` and re-read `package.json` before retrying. The `"typecheck"` script is defined as `"tsc --noEmit"` — if it appears missing, the issue is directory or shell context, not a missing package.

---

### CONTEXT

This ticket adds the "Completed Today" collapsible section below each priority bucket
and wires the midnight auto-archive logic. The bucket UI from Ticket 2-B is already
in place. This ticket modifies `PriorityBucket.tsx` and adds the archive logic — no
new major components.

All dependencies have already been installed by Claude Code. Do not run any installs.

Already installed and ready to import:
- shadcn `Collapsible` → `Collapsible`, `CollapsibleTrigger`, `CollapsibleContent`
- `lucide-react`
- `@tanstack/react-query` (`useArchiveTask` from `src/queries/tasks.ts`)
- `@supabase/supabase-js`

---

### SUB-TASKS

#### 1. ADD COMPLETED TODAY SECTION TO PriorityBucket.tsx

Modify `src/components/tasks/PriorityBucket.tsx`.

The data is already fetched by `useTasksByPriority()` which returns all tasks with
`status = 'active'`. For completed tasks, add a second query:

**useCompletedTodayTasks(userId: string)**
Add this hook to `src/queries/tasks.ts`.
Query key: `['tasks-completed-today', userId]`
Fetches tasks where `status = 'completed_today'`, ordered by `completed_at` desc.
Returns tasks grouped — the component will filter by priority client-side.

```ts
export function useCompletedTodayTasks(userId: string) {
  return useQuery({
    queryKey: ['tasks-completed-today', userId],
    queryFn: async () => {
      const { data } = await supabase
        .from('tasks')
        .select('*')
        .eq('user_id', userId)
        .eq('status', 'completed_today')
        .order('completed_at', { ascending: false })
        .throwOnError()
      return data
    },
  })
}
```

In `PriorityBucket.tsx`, below each priority section's task list, add:

```tsx
<Collapsible>
  <CollapsibleTrigger className="flex items-center gap-1 px-2 py-1 text-xs
    text-muted-foreground hover:text-foreground w-full">
    <ChevronRight className="h-3 w-3 transition-transform
      data-[state=open]:rotate-90" />
    Completed today ({completedForPriority.length})
  </CollapsibleTrigger>
  <CollapsibleContent>
    {completedForPriority.map(task => (
      <div key={task.id} className="flex items-center gap-2 px-2 py-1 opacity-50">
        <CheckCircle className="h-4 w-4 text-muted-foreground flex-shrink-0" />
        <span className="text-sm line-through text-muted-foreground">
          {task.title}
        </span>
      </div>
    ))}
    {completedForPriority.length === 0 && (
      <p className="text-xs text-muted-foreground px-2 py-1">
        Nothing completed in this list yet today.
      </p>
    )}
  </CollapsibleContent>
</Collapsible>
```

The collapsible must be collapsed by default (`defaultOpen={false}`).

Do not show any guilt-inducing copy. The strikethrough and muted styling is
sufficient visual treatment — no additional messaging about completion counts,
streaks, or productivity.

---

#### 2. ADD MIDNIGHT AUTO-ARCHIVE LOGIC

File path: `src/lib/archiveTasks.ts`

Create a utility function that archives tasks completed before today:

```ts
export async function archiveCompletedTasks(userId: string) {
  const today = new Date()
  today.setHours(0, 0, 0, 0)

  await supabase
    .from('tasks')
    .update({ status: 'archived' })
    .eq('user_id', userId)
    .eq('status', 'completed_today')
    .lt('completed_at', today.toISOString())
    .throwOnError()
}
```

Call this function on app load — not on a timer, not via `pg_cron` for MVP. Add it
to the authenticated route's loader or to a `useEffect` in `AppLayout.tsx` that runs
once on mount:

In `AppLayout.tsx`, add:

```ts
archiveCompletedTasks(userId)
  .then(() => {
    queryClient.invalidateQueries({ queryKey: ['tasks', userId] })
    queryClient.invalidateQueries({ queryKey: ['tasks-completed-today', userId] })
  })
  .catch(console.error)
```

Wrap in `useEffect` with `[userId]` dependency so it runs once per authenticated session:

```ts
useEffect(() => {
  if (userId) {
    archiveCompletedTasks(userId)
      .then(() => {
        queryClient.invalidateQueries({ queryKey: ['tasks', userId] })
        queryClient.invalidateQueries({ queryKey: ['tasks-completed-today', userId] })
      })
      .catch(console.error)
  }
}, [userId])
```

---

#### 3. VERIFY TONE COMPLIANCE IN ALL COMPLETED STATES

Review every string in `PriorityBucket.tsx`, `InlineTaskInput.tsx`, and `TaskPill.tsx`.

The following words must not appear anywhere — in JSX strings, aria-labels,
comments, variable names, or class names:

> `overdue`, `late`, `missed`, `behind`, `failed`, `incomplete`

The completed section trigger label must read "Completed today" — not
"Finished". "Done" is acceptable if preferred, but never anything implying
the user did not do enough.

---

### FILES TO CREATE OR MODIFY

- `src/queries/tasks.ts` — add `useCompletedTodayTasks` hook
- `src/components/tasks/PriorityBucket.tsx` — add Completed Today collapsible section
- `src/lib/archiveTasks.ts` — new file
- `src/components/layout/AppLayout.tsx` — add archive-on-mount effect

---

### HARD RULES REMINDER

- All Supabase calls end with `.throwOnError()`
- No tone violations — see list above
- Completed tasks get strikethrough + muted styling only. No guilt copy.
- Archive logic runs once on mount via `useEffect`, not on a polling interval

---

### ACCEPTANCE CRITERIA

- "Completed today" collapsible appears below each priority section
- Collapsible is closed by default
- Completing a task moves it from the active list to the completed section
  immediately (optimistic update from 2-A still applies)
- Completed tasks show strikethrough title and muted styling
- On next-day app load, tasks completed yesterday are archived and no longer
  appear in "Completed today"
- The count in the collapsible trigger reflects the actual number of completed
  tasks for that priority
- `npm run typecheck` exits with 0 errors
- `npm run lint` exits with 0 errors

---

### WHEN DONE

1. Run `npm run typecheck` — must exit with 0 errors
2. Run `npm run lint` — must exit with 0 errors
3. Manually test: complete a task → verify it moves to Completed Today →
   manually set `completed_at` to yesterday in Supabase dashboard → refresh app →
   verify it is gone from Completed Today
4. Update `memory-bank/activeContext.md`: mark 2-C complete, set Ticket 3-A as next
5. Update `memory-bank/progress.md`: check off Ticket 2-C and mark Phase 2 complete
6. Save the TanStack Query optimistic update pattern to
   `C:\dev\coding_templates\tanstack\optimistic-update.ts` if not already saved
   from Ticket 2-A

---

**Acceptance criteria (Phase 2):** Tasks create, reorder, and complete. Sections collapse. Completed today section renders per bucket. Data persists after refresh.

**TEMPLATE SAVE (2-A):** Extract TanStack Query hook pattern with optimistic update to `C:\dev\coding_templates\tanstack\query-hook.ts`

________________




# Phase 3: Calendar & Scheduling — Tickets 3-A through 3-F

---

## Ticket 3-A: FullCalendar Base Setup

Read memory-bank/activeContext.md, memory-bank/techContext.md, and CLAUDE.md before
doing anything else. Follow all Hard Rules in CLAUDE.md throughout this ticket.

---

### TICKET 3-A: FullCalendar Base Setup
Project folder: C:\dev\second_brain
Current phase: Phase 3 — Calendar & Scheduling

---

### VERIFICATION — READ BEFORE WRITING ANY CODE

**Step 1 — Confirm your working directory.**
Run `cd` (Windows CMD) or check your terminal header. It must show `C:\dev\second_brain`.
If it shows anything else (`C:\dev`, `C:\Users\robwa`, etc.), your terminal is in the wrong location.
Do not run any commands until this is correct. Open a new terminal rooted at `C:\dev\second_brain` if needed.

**Step 2 — Install the FullCalendar packages (this ticket only — these are the ONLY installs in Phase 3).**
Run this single command from `C:\dev\second_brain`:

```
npm install @fullcalendar/react @fullcalendar/timegrid @fullcalendar/interaction @fullcalendar/daygrid
```

The `.npmrc` file already contains `legacy-peer-deps=true`, so this install will resolve cleanly. Do not add `--legacy-peer-deps` manually — it is already handled.

After installing, do not run any further `npm install` commands for any other ticket in Phase 3. All other dependencies are pre-installed.

**Step 3 — Do not run `cd` commands.** You are already in the project root: `C:\dev\second_brain`. Running `cd second_brain` will silently drop you into a parent directory with a different `package.json` — this causes "Missing script" errors.

To verify your work, run these two commands only:

```
npm run typecheck   ← must exit with 0 errors
npm run lint        ← must exit with 0 errors
```

Do **NOT** run `npm run dev`. Claude Code will review and fix anything remaining.

If either command reports a missing script, your terminal is not in `C:\dev\second_brain`. Check with `cd` first.

---

### CONTEXT

This ticket sets up the FullCalendar base component that all calendar features in
Phase 3 depend on. No drag, no zones, no data layer yet — just the calendar rendering
correctly with the right view configuration. Get this right before any other Phase 3
ticket begins.

---

### SUB-TASKS

#### 1. CREATE CalendarView.tsx

File path: `src/components/calendar/CalendarView.tsx`

```tsx
import FullCalendar from '@fullcalendar/react'
import timeGridPlugin from '@fullcalendar/timegrid'
import interactionPlugin from '@fullcalendar/interaction'
import dayGridPlugin from '@fullcalendar/daygrid'

export function CalendarView() {
  return (
    <FullCalendar
      plugins={[timeGridPlugin, interactionPlugin, dayGridPlugin]}
      initialView="timeGridThreeDay"
      views={{
        timeGridThreeDay: {
          type: 'timeGrid',
          duration: { days: 3 },
          buttonText: '3 day',
        },
      }}
      nowIndicator={true}
      allDaySlot={true}
      slotMinTime="06:00:00"
      slotMaxTime="22:00:00"
      headerToolbar={{
        left: 'prev,next today',
        center: 'title',
        right: 'timeGridDay,timeGridThreeDay,dayGridMonth',
      }}
      height="100%"
    />
  )
}
```

#### 2. WIRE CalendarView INTO THE APP LAYOUT

Place `<CalendarView />` in the main content area of the app shell (right of the
sidebar). Replace the placeholder route content from Ticket 2-B's `/tasks` route
or whatever placeholder currently occupies the main panel.

Add a comment: `// TODO: Ticket 3-D — eventReceive wired here for bucket drag`

---

### FILES TO CREATE OR MODIFY

- `src/components/calendar/CalendarView.tsx` — new file
- App layout/route file — wire CalendarView into main panel

---

### HARD RULES REMINDER

- No tone violations in variable names, comments, or string values
- Do not run additional `npm install` commands beyond the one specified above

---

### ACCEPTANCE CRITERIA

- Calendar renders in the main panel with the 3-day view as default
- Three-day, day, and month view buttons appear in the toolbar
- `nowIndicator` is visible
- `allDaySlot` row is visible at the top
- Time slots run from 06:00 to 22:00
- `npm run typecheck` exits with 0 errors
- `npm run lint` exits with 0 errors

---

### WHEN DONE

1. Run `npm run typecheck` — must exit with 0 errors
2. Run `npm run lint` — must exit with 0 errors
3. Update `memory-bank/activeContext.md`: mark 3-A complete, set 3-B as current ticket
4. Update `memory-bank/progress.md`: check off Ticket 3-A

---
---

## Ticket 3-B: Morning / Afternoon / Evening Zones

Read memory-bank/activeContext.md, memory-bank/techContext.md, and CLAUDE.md before
doing anything else. Follow all Hard Rules in CLAUDE.md throughout this ticket.

---

### TICKET 3-B: Morning / Afternoon / Evening Zones
Project folder: C:\dev\second_brain
Current phase: Phase 3 — Calendar & Scheduling

---

### VERIFICATION — READ BEFORE WRITING ANY CODE

**Step 1 — Confirm your working directory.**
Run `cd` (Windows CMD) or check your terminal header. It must show `C:\dev\second_brain`.
If it shows anything else, your terminal is in the wrong location. Do not proceed until corrected.

**Step 2 — Do not run `npm install`.** All packages are pre-installed.
The `.npmrc` file already contains `legacy-peer-deps=true`.

**Step 3 — Do not run `cd` commands.** You are already in the project root: `C:\dev\second_brain`.

To verify your work, run these two commands only:

```
npm run typecheck   ← must exit with 0 errors
npm run lint        ← must exit with 0 errors
```

Do **NOT** run `npm run dev`. Claude Code will review and fix anything remaining.

If either command reports a missing script, your terminal is not in `C:\dev\second_brain`. Check with `cd` first.

---

### CONTEXT

This ticket adds visual zone labels (Morning, Afternoon, Evening) to the FullCalendar
time grid using the `slotLaneContent` render prop. No new packages needed — this is
purely a render customization on the existing CalendarView from Ticket 3-A.

---

### SUB-TASKS

#### 1. ADD slotLaneContent TO CalendarView.tsx

Modify `src/components/calendar/CalendarView.tsx`.

Add the `slotLaneContent` prop to `<FullCalendar>`:

```tsx
slotLaneContent={(arg) => {
  const ms = arg.time.milliseconds

  const NOON = 12 * 60 * 60 * 1000       // 43200000ms
  const EVENING = 18 * 60 * 60 * 1000    // 64800000ms

  // Only inject label at the start of each zone, not every slot
  const isMorningStart = ms === 6 * 60 * 60 * 1000   // 06:00
  const isAfternoonStart = ms === NOON                 // 12:00
  const isEveningStart = ms === EVENING                // 18:00

  if (isMorningStart) {
    return (
      <div className="zone-label zone-label--morning">
        Morning
      </div>
    )
  }
  if (isAfternoonStart) {
    return (
      <div className="zone-label zone-label--afternoon">
        Afternoon
      </div>
    )
  }
  if (isEveningStart) {
    return (
      <div className="zone-label zone-label--evening">
        Evening
      </div>
    )
  }
  return null
}}
```

#### 2. ADD ZONE TINT STYLES

Add tint styles via CSS. Place in `src/styles/calendar.css` (create if it doesn't
exist) and import it in `CalendarView.tsx`.

The tint is a low-opacity background only — no borders, no lines.

```css
/* Zone background tints — applied via slotLane rows */
.fc-timegrid-slot[data-time^="06"],
.fc-timegrid-slot[data-time^="07"],
.fc-timegrid-slot[data-time^="08"],
.fc-timegrid-slot[data-time^="09"],
.fc-timegrid-slot[data-time^="10"],
.fc-timegrid-slot[data-time^="11"] {
  background-color: rgba(250, 240, 200, 0.08);
}

.fc-timegrid-slot[data-time^="12"],
.fc-timegrid-slot[data-time^="13"],
.fc-timegrid-slot[data-time^="14"],
.fc-timegrid-slot[data-time^="15"],
.fc-timegrid-slot[data-time^="16"],
.fc-timegrid-slot[data-time^="17"] {
  background-color: rgba(200, 220, 255, 0.08);
}

.fc-timegrid-slot[data-time^="18"],
.fc-timegrid-slot[data-time^="19"],
.fc-timegrid-slot[data-time^="20"],
.fc-timegrid-slot[data-time^="21"] {
  background-color: rgba(180, 160, 220, 0.08);
}

/* Zone label styling */
.zone-label {
  font-size: 0.6rem;
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  opacity: 0.35;
  pointer-events: none;
  user-select: none;
  padding-left: 4px;
}
```

---

### FILES TO CREATE OR MODIFY

- `src/components/calendar/CalendarView.tsx` — add `slotLaneContent` prop
- `src/styles/calendar.css` — new file (zone tints and label styles)

---

### HARD RULES REMINDER

- Tint is background-color only — no borders, no lines, no shadows
- Zone labels are informational — do not use language implying urgency or pressure
- No tone violations

---

### ACCEPTANCE CRITERIA

- "Morning", "Afternoon", "Evening" labels appear at 06:00, 12:00, and 18:00 rows
- Labels are visually subtle (low opacity)
- Each zone has a distinct very-low-opacity background tint
- No borders or lines added between zones
- `npm run typecheck` exits with 0 errors
- `npm run lint` exits with 0 errors

---

### WHEN DONE

1. Run `npm run typecheck` — must exit with 0 errors
2. Run `npm run lint` — must exit with 0 errors
3. Update `memory-bank/activeContext.md`: mark 3-B complete, set 3-C as current ticket
4. Update `memory-bank/progress.md`: check off Ticket 3-B

---
---

## Ticket 3-C: Calendar Data Layer

Read memory-bank/activeContext.md, memory-bank/techContext.md, and CLAUDE.md before
doing anything else. Follow all Hard Rules in CLAUDE.md throughout this ticket.

---

### TICKET 3-C: Calendar Data Layer
Project folder: C:\dev\second_brain
Current phase: Phase 3 — Calendar & Scheduling

---

### VERIFICATION — READ BEFORE WRITING ANY CODE

**Step 1 — Confirm your working directory.**
Run `cd` (Windows CMD) or check your terminal header. It must show `C:\dev\second_brain`.
If it shows anything else, your terminal is in the wrong location. Do not proceed until corrected.

**Step 2 — Do not run `npm install`.** All packages are pre-installed.
The `.npmrc` file already contains `legacy-peer-deps=true`.

**Step 3 — Do not run `cd` commands.** You are already in the project root: `C:\dev\second_brain`.

To verify your work, run these two commands only:

```
npm run typecheck   ← must exit with 0 errors
npm run lint        ← must exit with 0 errors
```

Do **NOT** run `npm run dev`. Claude Code will review and fix anything remaining.

If either command reports a missing script, your terminal is not in `C:\dev\second_brain`. Check with `cd` first.

---

### CONTEXT

This ticket builds the data layer for calendar blocks. No UI changes — only hooks
and callbacks. Ticket 3-D wires the drag interaction; this ticket only provides the
query/mutation layer that 3-D and 3-E will consume.

Already installed and ready to import:
- `@supabase/supabase-js` (client already configured)
- `@tanstack/react-query` → `useQuery`, `useMutation`, `useQueryClient`

---

### SUB-TASKS

#### 1. BUILD THE CALENDAR BLOCK QUERY HOOKS

File path: `src/queries/calendarBlocks.ts`

All Supabase calls end with `.throwOnError()`.

**useCalendarBlocks(dateRange: { start: string, end: string })**
Query key: `['calendar-blocks', dateRange.start, dateRange.end]`

```ts
export function useCalendarBlocks(dateRange: { start: string; end: string }) {
  return useQuery({
    queryKey: ['calendar-blocks', dateRange.start, dateRange.end],
    queryFn: async () => {
      const { data } = await supabase
        .from('calendar_blocks')
        .select('*')
        .gte('start_time', dateRange.start)
        .lte('end_time', dateRange.end)
        .throwOnError()
      return data
    },
  })
}
```

**useCreateBlock()**
Inserts a new calendar block.
Input: `{ title, start_time, end_time, block_type, task_id?, user_id }`
On success: invalidate `['calendar-blocks']`

**useUpdateBlock()**
Updates an existing block.
Input: `{ blockId: string, updates: Partial<CalendarBlock> }`
On success: invalidate `['calendar-blocks']`
Used by `eventDrop` (new `start_time` + `end_time`) and `eventResize` (new `end_time`).

**useDeleteBlock()**
Deletes a block by ID.
Input: `blockId: string`
On success: invalidate `['calendar-blocks']`

`CalendarBlock` type comes from `src/types/database.types.ts`. Use the generated
type — do not define a manual type.

#### 2. WIRE EVENT CALLBACKS INTO CalendarView.tsx

Modify `src/components/calendar/CalendarView.tsx`.

Import and call the hooks. Add these callback props to `<FullCalendar>`:

```tsx
// eventDrop — user drags an existing event to a new time
eventDrop={(info) => {
  updateBlock({
    blockId: info.event.id,
    updates: {
      start_time: info.event.startStr,
      end_time: info.event.endStr,
    },
  })
}}

// eventResize — user resizes an existing event
eventResize={(info) => {
  updateBlock({
    blockId: info.event.id,
    updates: {
      end_time: info.event.endStr,
    },
  })
}}

// eventReceive — block dragged in from bucket (Ticket 3-D wires the source)
// Add a comment: // TODO: Ticket 3-D — full eventReceive logic added here
eventReceive={(info) => {
  console.log('eventReceive stub — wired in Ticket 3-D', info.event)
  info.revert() // revert until 3-D is wired
}}
```

Pass `events` from `useCalendarBlocks` to the `events` prop, mapping each block to
FullCalendar's event shape:
```ts
events={(calendarBlocks ?? []).map(block => ({
  id: block.id,
  title: block.title,
  start: block.start_time,
  end: block.end_time,
  extendedProps: { block_type: block.block_type, task_id: block.task_id },
}))}
```

---

### FILES TO CREATE OR MODIFY

- `src/queries/calendarBlocks.ts` — new file
- `src/components/calendar/CalendarView.tsx` — add event callbacks and data binding

---

### HARD RULES REMINDER

- All Supabase calls end with `.throwOnError()`
- No tone violations
- `eventReceive` must revert (stub) until Ticket 3-D fully wires it — do not leave
  a half-wired state that creates orphaned blocks

---

### ACCEPTANCE CRITERIA

- `useCalendarBlocks`, `useCreateBlock`, `useUpdateBlock`, `useDeleteBlock` are all
  exported from `src/queries/calendarBlocks.ts`
- `eventDrop` saves new `start_time` and `end_time` to Supabase
- `eventResize` saves new `end_time` to Supabase
- `eventReceive` stub reverts the drag and logs — does not create blocks yet
- Calendar renders existing blocks from Supabase
- `npm run typecheck` exits with 0 errors
- `npm run lint` exits with 0 errors

---

### WHEN DONE

1. Run `npm run typecheck` — must exit with 0 errors
2. Run `npm run lint` — must exit with 0 errors
3. Update `memory-bank/activeContext.md`: mark 3-C complete, set 3-D as current ticket
4. Update `memory-bank/progress.md`: check off Ticket 3-C

---
---

## Ticket 3-D: Drag from Bucket to Calendar

Read memory-bank/activeContext.md, memory-bank/techContext.md, and CLAUDE.md before
doing anything else. Follow all Hard Rules in CLAUDE.md throughout this ticket.

---

### TICKET 3-D: Drag from Bucket to Calendar
Project folder: C:\dev\second_brain
Current phase: Phase 3 — Calendar & Scheduling

---

### VERIFICATION — READ BEFORE WRITING ANY CODE

**Step 1 — Confirm your working directory.**
Run `cd` (Windows CMD) or check your terminal header. It must show `C:\dev\second_brain`.
If it shows anything else, your terminal is in the wrong location. Do not proceed until corrected.

**Step 2 — Do not run `npm install`.** All packages are pre-installed.
The `.npmrc` file already contains `legacy-peer-deps=true`.

**Step 3 — Do not run `cd` commands.** You are already in the project root: `C:\dev\second_brain`.

To verify your work, run these two commands only:

```
npm run typecheck   ← must exit with 0 errors
npm run lint        ← must exit with 0 errors
```

Do **NOT** run `npm run dev`. Claude Code will review and fix anything remaining.

If either command reports a missing script, your terminal is not in `C:\dev\second_brain`. Check with `cd` first.

---

### CONTEXT

This ticket wires the drag-from-bucket-to-calendar interaction. The `bucketRef` and
`task-pill` data attributes were added in Ticket 2-B specifically for this. The
`eventReceive` stub from Ticket 3-C is replaced here with the full implementation.

Do **NOT** use dnd-kit or react-dnd — they conflict with FullCalendar's event model.
Use FullCalendar's built-in `Draggable` from `@fullcalendar/interaction` only.

Already installed and ready to import:
- `@fullcalendar/interaction` → `Draggable`
- `src/lib/taskConstants.ts` → `BLOCK_SIZE_DURATIONS`
- `src/queries/calendarBlocks.ts` → `useCreateBlock`
- `src/queries/tasks.ts` → `useCompleteTask`

---

### SUB-TASKS

#### 1. INITIALIZE FullCalendar Draggable IN PriorityBucket.tsx

Modify `src/components/tasks/PriorityBucket.tsx`.

Import `Draggable` from `@fullcalendar/interaction`.

In a `useEffect` that runs once on mount, initialize `Draggable` on `bucketRef`:

```ts
useEffect(() => {
  if (!bucketRef.current) return

  const draggable = new Draggable(bucketRef.current, {
    itemSelector: '.task-pill',
    eventData: (el) => ({
      id: el.dataset.taskId,
      title: el.dataset.title,
      duration: BLOCK_SIZE_DURATIONS[el.dataset.blockSize as 'S' | 'M' | 'L'],
    }),
  })

  return () => draggable.destroy()
}, [])
```

The `bucketRef` was created in Ticket 2-B. The `.task-pill` class and all four
`data-*` attributes on `TaskPill` were added in Ticket 2-B. Do not modify them.

#### 2. REPLACE eventReceive STUB IN CalendarView.tsx

Modify `src/components/calendar/CalendarView.tsx`.

Replace the stub `eventReceive` (from Ticket 3-C) with the full implementation:

```tsx
eventReceive={(info) => {
  const taskId = info.event.id
  const title = info.event.title
  const start = info.event.startStr
  const end = info.event.endStr

  createBlock({
    user_id: userId,
    title,
    start_time: start,
    end_time: end,
    block_type: 'task',
    task_id: taskId,
  })

  completeTask(taskId)

  // Remove the ghost event FullCalendar added — the real block comes from the DB
  info.event.remove()
}}
```

`createBlock` comes from `useCreateBlock()`. `completeTask` comes from
`useCompleteTask()`. Both are already in place from Phase 2 and Ticket 3-C.

`userId` comes from the auth context — use the same pattern as the rest of the app.

---

### FILES TO MODIFY

- `src/components/tasks/PriorityBucket.tsx` — add Draggable init in useEffect
- `src/components/calendar/CalendarView.tsx` — replace eventReceive stub

---

### HARD RULES REMINDER

- Do NOT use dnd-kit or react-dnd
- Do NOT leave the `Draggable` instance without a cleanup (`draggable.destroy()`)
- The `.task-pill` selector and data attributes must not be changed — they are
  load-bearing from Ticket 2-B
- No tone violations

---

### ACCEPTANCE CRITERIA

- Dragging a TaskPill from the bucket onto the calendar creates a calendar block
- The task moves to "Completed Today" in the bucket immediately (optimistic update)
- The ghost event is removed after drop; the real block renders from the DB
- `eventDrop` and `eventResize` from Ticket 3-C still work correctly
- `npm run typecheck` exits with 0 errors
- `npm run lint` exits with 0 errors

---

### WHEN DONE

1. Run `npm run typecheck` — must exit with 0 errors
2. Run `npm run lint` — must exit with 0 errors
3. Manually test: drag a task pill onto the calendar, confirm block appears and
   task moves to Completed Today
4. Update `memory-bank/activeContext.md`: mark 3-D complete, set 3-E as current ticket
5. Update `memory-bank/progress.md`: check off Ticket 3-D

---
---

## Ticket 3-E: Create Blocks Directly on Calendar

Read memory-bank/activeContext.md, memory-bank/techContext.md, and CLAUDE.md before
doing anything else. Follow all Hard Rules in CLAUDE.md throughout this ticket.

---

### TICKET 3-E: Create Blocks Directly on Calendar
Project folder: C:\dev\second_brain
Current phase: Phase 3 — Calendar & Scheduling

---

### VERIFICATION — READ BEFORE WRITING ANY CODE

**Step 1 — Confirm your working directory.**
Run `cd` (Windows CMD) or check your terminal header. It must show `C:\dev\second_brain`.
If it shows anything else, your terminal is in the wrong location. Do not proceed until corrected.

**Step 2 — Do not run `npm install`.** All packages are pre-installed.
The `.npmrc` file already contains `legacy-peer-deps=true`.

**Step 3 — Do not run `cd` commands.** You are already in the project root: `C:\dev\second_brain`.

To verify your work, run these two commands only:

```
npm run typecheck   ← must exit with 0 errors
npm run lint        ← must exit with 0 errors
```

Do **NOT** run `npm run dev`. Claude Code will review and fix anything remaining.

If either command reports a missing script, your terminal is not in `C:\dev\second_brain`. Check with `cd` first.

---

### CONTEXT

This ticket adds click-to-create for calendar blocks directly on the time grid.
The user clicks and drags on an empty slot, a small Dialog appears for title and
block type, and confirming calls `useCreateBlock()`.

Already installed and ready to import:
- shadcn `Dialog`, `DialogContent`, `DialogHeader`, `DialogTitle` → from `@/components/ui/dialog`
- shadcn `Button`, `Input` → from `@/components/ui/`
- `src/queries/calendarBlocks.ts` → `useCreateBlock`

---

### SUB-TASKS

#### 1. ADD selectable AND select CALLBACK TO CalendarView.tsx

Modify `src/components/calendar/CalendarView.tsx`.

Add `selectable={true}` to `<FullCalendar>`.

Add state to hold the pending selection:

```ts
const [pendingBlock, setPendingBlock] = useState<{
  start: string
  end: string
} | null>(null)
```

Add the `select` callback:

```tsx
select={(info) => {
  setPendingBlock({ start: info.startStr, end: info.endStr })
}}
```

#### 2. BUILD THE CREATE BLOCK DIALOG

Add inline (in `CalendarView.tsx`) or extract to
`src/components/calendar/CreateBlockDialog.tsx` if preferred.

```tsx
<Dialog open={!!pendingBlock} onOpenChange={(open) => { if (!open) setPendingBlock(null) }}>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>New block</DialogTitle>
    </DialogHeader>

    <Input
      placeholder="Title"
      value={title}
      onChange={(e) => setTitle(e.target.value)}
      onKeyDown={(e) => { if (e.key === 'Enter') handleConfirm() }}
      autoFocus
    />

    {/* Block type selector */}
    <div className="flex gap-2 mt-2">
      {['focus', 'task', 'event'].map((type) => (
        <button
          key={type}
          onClick={() => setBlockType(type)}
          className={blockType === type ? 'ring-2 ring-primary rounded px-2 py-1 text-sm capitalize' : 'px-2 py-1 text-sm capitalize'}
        >
          {type}
        </button>
      ))}
    </div>

    <div className="flex justify-end gap-2 mt-4">
      <Button variant="outline" onClick={() => setPendingBlock(null)}>Cancel</Button>
      <Button onClick={handleConfirm} disabled={!title.trim()}>Add</Button>
    </div>
  </DialogContent>
</Dialog>
```

`handleConfirm` calls `useCreateBlock()` with:
```ts
{
  user_id: userId,
  title: title.trim(),
  start_time: pendingBlock.start,
  end_time: pendingBlock.end,
  block_type: blockType,
}
```
After confirming: clear `title`, `blockType`, and `pendingBlock`.

State needed:
```ts
const [title, setTitle] = useState('')
const [blockType, setBlockType] = useState<string>('focus')
```

Do NOT use `<form>` tags. Use controlled inputs with `onKeyDown`.

---

### FILES TO CREATE OR MODIFY

- `src/components/calendar/CalendarView.tsx` — add `selectable`, `select` callback, Dialog
- `src/components/calendar/CreateBlockDialog.tsx` — optional extraction

---

### HARD RULES REMINDER

- No HTML `<form>` tags — use controlled inputs with `onKeyDown`
- No tone violations
- Block type labels (`focus`, `task`, `event`) are informational — no urgency language

---

### ACCEPTANCE CRITERIA

- Clicking and dragging on an empty calendar slot opens the Dialog
- Pressing Enter or clicking Add with a title creates the block
- Pressing Escape or clicking Cancel dismisses without creating
- New block appears on calendar immediately after create
- `npm run typecheck` exits with 0 errors
- `npm run lint` exits with 0 errors

---

### WHEN DONE

1. Run `npm run typecheck` — must exit with 0 errors
2. Run `npm run lint` — must exit with 0 errors
3. Manually test: click-drag on calendar → Dialog opens → confirm → block appears
4. Update `memory-bank/activeContext.md`: mark 3-E complete, set 3-F as current ticket
5. Update `memory-bank/progress.md`: check off Ticket 3-E

---
---

## Ticket 3-F: Google Calendar Sync

Read memory-bank/activeContext.md, memory-bank/techContext.md, and CLAUDE.md before
doing anything else. Follow all Hard Rules in CLAUDE.md throughout this ticket.

---

### TICKET 3-F: Google Calendar Sync
Project folder: C:\dev\second_brain
Current phase: Phase 3 — Calendar & Scheduling

---

### VERIFICATION — READ BEFORE WRITING ANY CODE

**Step 1 — Confirm your working directory.**
Run `cd` (Windows CMD) or check your terminal header. It must show `C:\dev\second_brain`.
If it shows anything else, your terminal is in the wrong location. Do not proceed until corrected.

**Step 2 — Do not run `npm install`.** All packages are pre-installed.
The `.npmrc` file already contains `legacy-peer-deps=true`.

**Step 3 — Do not run `cd` commands.** You are already in the project root: `C:\dev\second_brain`.

To verify your work, run these two commands only:

```
npm run typecheck   ← must exit with 0 errors
npm run lint        ← must exit with 0 errors
```

Do **NOT** run `npm run dev`. Claude Code will review and fix anything remaining.

If either command reports a missing script, your terminal is not in `C:\dev\second_brain`. Check with `cd` first.

---

### CONTEXT

This ticket adds Google Calendar sync via a TanStack Start server function. On app
load, events are fetched for ±14 days and upserted into `calendar_blocks`. All
create/update/delete operations on `calendar_blocks` mirror to Google Calendar.
Token refresh happens on every API call.

---

### SUB-TASKS

#### 1. CREATE THE GOOGLE CALENDAR SERVER FUNCTION

File path: `src/server/googleCalendar.ts`

```ts
import { createServerFn } from '@tanstack/start'
import { google } from 'googleapis'

const getOAuth2Client = (accessToken: string, refreshToken: string) => {
  const client = new google.auth.OAuth2(
    process.env.GOOGLE_CLIENT_ID,
    process.env.GOOGLE_CLIENT_SECRET,
    process.env.GOOGLE_REDIRECT_URI,
  )
  client.setCredentials({ access_token: accessToken, refresh_token: refreshToken })
  return client
}

// Fetch and upsert Google Calendar events for ±14 days
export const syncGoogleCalendar = createServerFn('POST', async (
  { userId, accessToken, refreshToken }: {
    userId: string
    accessToken: string
    refreshToken: string
  }
) => {
  const auth = getOAuth2Client(accessToken, refreshToken)

  // Refresh token on every call
  const { credentials } = await auth.refreshAccessToken()
  auth.setCredentials(credentials)

  // Persist refreshed token to profiles
  if (credentials.refresh_token) {
    await supabase
      .from('profiles')
      .update({ google_refresh_token: credentials.refresh_token })
      .eq('id', userId)
      .throwOnError()
  }

  const calendar = google.calendar({ version: 'v3', auth })

  const now = new Date()
  const timeMin = new Date(now.getTime() - 14 * 24 * 60 * 60 * 1000).toISOString()
  const timeMax = new Date(now.getTime() + 14 * 24 * 60 * 60 * 1000).toISOString()

  const { data } = await calendar.events.list({
    calendarId: 'primary',
    timeMin,
    timeMax,
    singleEvents: true,
    orderBy: 'startTime',
  })

  const events = data.items ?? []

  // Upsert into calendar_blocks matching on google_event_id
  for (const event of events) {
    if (!event.id || !event.summary) continue
    await supabase
      .from('calendar_blocks')
      .upsert({
        user_id: userId,
        title: event.summary,
        start_time: event.start?.dateTime ?? event.start?.date,
        end_time: event.end?.dateTime ?? event.end?.date,
        block_type: 'event',
        google_event_id: event.id,
        is_synced: true,
      }, { onConflict: 'google_event_id' })
      .throwOnError()
  }

  return { synced: events.length }
})

// Mirror a create to Google Calendar
export const createGoogleEvent = createServerFn('POST', async (
  { block, accessToken, refreshToken }: {
    block: { title: string; start_time: string; end_time: string }
    accessToken: string
    refreshToken: string
  }
) => {
  const auth = getOAuth2Client(accessToken, refreshToken)
  const { credentials } = await auth.refreshAccessToken()
  auth.setCredentials(credentials)

  const calendar = google.calendar({ version: 'v3', auth })

  const { data } = await calendar.events.insert({
    calendarId: 'primary',
    requestBody: {
      summary: block.title,
      start: { dateTime: block.start_time },
      end: { dateTime: block.end_time },
    },
  })

  return { googleEventId: data.id }
})

// Mirror an update to Google Calendar
export const updateGoogleEvent = createServerFn('POST', async (
  { googleEventId, updates, accessToken, refreshToken }: {
    googleEventId: string
    updates: { title?: string; start_time?: string; end_time?: string }
    accessToken: string
    refreshToken: string
  }
) => {
  const auth = getOAuth2Client(accessToken, refreshToken)
  const { credentials } = await auth.refreshAccessToken()
  auth.setCredentials(credentials)

  const calendar = google.calendar({ version: 'v3', auth })

  await calendar.events.patch({
    calendarId: 'primary',
    eventId: googleEventId,
    requestBody: {
      ...(updates.title && { summary: updates.title }),
      ...(updates.start_time && { start: { dateTime: updates.start_time } }),
      ...(updates.end_time && { end: { dateTime: updates.end_time } }),
    },
  })
})

// Mirror a delete to Google Calendar
export const deleteGoogleEvent = createServerFn('POST', async (
  { googleEventId, accessToken, refreshToken }: {
    googleEventId: string
    accessToken: string
    refreshToken: string
  }
) => {
  const auth = getOAuth2Client(accessToken, refreshToken)
  const { credentials } = await auth.refreshAccessToken()
  auth.setCredentials(credentials)

  const calendar = google.calendar({ version: 'v3', auth })

  await calendar.events.delete({
    calendarId: 'primary',
    eventId: googleEventId,
  })
})
```

#### 2. CALL syncGoogleCalendar ON APP LOAD

In `AppLayout.tsx`, alongside the archive-on-mount effect from Ticket 2-C, add:

```ts
useEffect(() => {
  if (userId && googleAccessToken && googleRefreshToken) {
    syncGoogleCalendar({ userId, accessToken: googleAccessToken, refreshToken: googleRefreshToken })
      .then(() => {
        queryClient.invalidateQueries({ queryKey: ['calendar-blocks'] })
      })
      .catch(console.error)
  }
}, [userId])
```

Get `googleAccessToken` and `googleRefreshToken` from the user's profile or session —
use whatever pattern the auth context provides.

#### 3. MIRROR MUTATIONS IN calendarBlocks.ts

In `src/queries/calendarBlocks.ts`, update `useCreateBlock`, `useUpdateBlock`, and
`useDeleteBlock` to call the corresponding server functions after Supabase succeeds:

- `useCreateBlock` → after insert, call `createGoogleEvent` → update the block's
  `google_event_id` with the returned ID
- `useUpdateBlock` → if block has `google_event_id`, call `updateGoogleEvent`
- `useDeleteBlock` → if block has `google_event_id`, call `deleteGoogleEvent`

#### 4. SHOW GOOGLE LOGO ON SYNCED EVENTS

In `CalendarView.tsx`, use the `eventContent` render prop to show a small Google
logo on blocks where `extendedProps.is_synced === true`:

```tsx
eventContent={(arg) => (
  <div className="fc-event-main-frame">
    <div className="fc-event-title">{arg.event.title}</div>
    {arg.event.extendedProps.is_synced && (
      <span className="text-[10px] opacity-60 ml-1">G</span>
    )}
  </div>
)}
```

Use a proper Google "G" logo SVG or icon if one is available in the project assets.
A plain "G" text label is acceptable as a fallback.

---

### FILES TO CREATE OR MODIFY

- `src/server/googleCalendar.ts` — new file
- `src/queries/calendarBlocks.ts` — add Google mirror calls to mutations
- `src/components/calendar/CalendarView.tsx` — add `eventContent` for sync indicator
- `src/components/layout/AppLayout.tsx` — add sync-on-mount effect

---

### HARD RULES REMINDER

- All Supabase calls end with `.throwOnError()`
- Token refresh runs on every Google API call — never skip it
- No tone violations
- `is_synced: true` is set only on blocks that came from Google — never on locally
  created blocks unless they have been successfully pushed to Google

---

### ACCEPTANCE CRITERIA

- On app load, Google Calendar events for ±14 days appear in the calendar
- Synced events show the Google "G" indicator
- Creating a block mirrors to Google Calendar and stores `google_event_id`
- Moving or resizing a synced block updates the Google event
- Deleting a synced block deletes the Google event
- Token refresh runs and updated `refresh_token` is persisted to `profiles`
- `npm run typecheck` exits with 0 errors
- `npm run lint` exits with 0 errors

---

### WHEN DONE

1. Run `npm run typecheck` — must exit with 0 errors
2. Run `npm run lint` — must exit with 0 errors
3. Manually test: connect Google account → verify events appear → create a block →
   verify it appears in Google Calendar
4. Update `memory-bank/activeContext.md`: mark 3-F complete, mark Phase 3 complete
5. Update `memory-bank/progress.md`: check off Ticket 3-F and mark Phase 3 complete
6. Save `src/server/googleCalendar.ts` to
   `C:\dev\coding_templates\google-calendar\calendar-server-fn.ts` — replace all
   project-specific values (`userId`, table names, env var names) with
   `[USER_ID]`, `[TABLE_NAME]`, `[ENV_VAR]` placeholders

---

**Acceptance criteria (Phase 3):** Calendar renders 3-day view. Zone labels visible. Tasks drag from bucket to calendar and create blocks. Move and resize save. Google events appear after connect.

**TEMPLATE SAVE (3-F):** Extract `src/server/googleCalendar.ts` to `C:\dev\coding_templates\google-calendar\calendar-server-fn.ts` with all project-specific values replaced.


________________




Phase 4: Pages, Folders & Journal (3–4 sessions)
Ticket 4-A: Folder tree


* Install: npm install react-arborist
* FolderTree.tsx in left sidebar panel
* Custom FolderNode renderer: folder icon, page icon, title, ... context menu
* Wire onCreate, onRename, onMove, onDelete to Supabase mutations
* Create page button → useCreatePage() → navigate to /pages/[id]
* position field updated on drag reorder


Ticket 4-B: BlockNote page editor


* Install: npm install @blocknote/react @blocknote/mantine
* PageView.tsx at route /pages/$pageId
* useCreateBlockNote({ initialContent: page.content })
* onChange → debounced autosave (800ms) → useUpdatePage()
* Show "Saving..." spinner during autosave in top bar
* Custom slash commands: /link (Phase 6), /ai (Phase 7)
* Page title: inline editable <h1> above editor, same autosave pattern


Ticket 4-C: Journal section


* Route: /journal
* Auto-create journal page for current day if none exists: title = "Journal — [Day], [Month] [D]"
* Cross-day title logic: "Journal — [Mon] – [Fri], [Month] [D–D]" (same month), "[Month D] – [Month D]" (cross-month)
* Journal entries listed by date in left panel (most recent first)
* Same BlockNote editor as pages
* Write unit tests in Vitest for getJournalTitle(createdAt, updatedAt)


Acceptance criteria: Folder tree renders. Drag reorder works. Pages save. Journal auto-creates daily entry. Title logic unit tests pass.


TEMPLATE SAVE (4-B): Extract autosave hook to C:\dev\coding_templates\components\autosave-hook.ts


________________




Phase 5: Tables (3–4 sessions)
Ticket 5-A: Table data layer


* src/queries/tables.ts: useTables(), useCreateTable(), useTableSchema($tableId), useTableRows($tableId)
* src/queries/tableRows.ts: useCreateRow(), useUpdateRow(), useDeleteRow()
* Column types: text, checkbox, select, date, number, url


Ticket 5-B: Table schema builder


* Route: /tables/$tableId/settings
* Add, rename, reorder, delete columns
* Column type selector (dropdown)
* select type: manage option list inline
* Save schema to tables_schema.columns JSONB


Ticket 5-C: Table view with inline editing


* Route: /tables/$tableId
* Use TanStack Table + shadcn Data Table as base
* Build column renderers per type:
   * text: <Input> on click
   * checkbox: <Checkbox> toggle
   * select: shadcn <Select> dropdown
   * date: shadcn <Calendar> popover
   * number: <Input type="number">
   * url: <a> tag display, <Input> on edit
* Sort, filter, column visibility via TanStack Table built-ins
* New row: "+ Add row" button at bottom → inline empty row


Ticket 5-D: Row detail page


* Route: /tables/$tableId/rows/$rowId
* Show all fields in a vertical form layout
* BlockNote editor for row notes (same autosave pattern)
* "← Back to table" breadcrumb


Acceptance criteria: Tables create with custom columns. All cell types edit inline. Rows save on blur. Row detail page opens.


________________




Phase 6: Universal Linking & Global Search (2 sessions)
Ticket 6-A: ⌘K global search


* Install via npx shadcn@latest add command
* CommandDialog.tsx: opens on ⌘K / Ctrl+K via useEffect keydown listener
* Search groups: Pages, Tasks, Folders, Table rows
* Each group queries Supabase ilike on title/content
* Keyboard nav with arrow keys, Enter to navigate
* Show type icon + title + path in each result


Ticket 6-B: Inline page linking in BlockNote


* Register custom LinkChip inline content type in BlockNote
* Displays: [📄 Page Title] chip inline in editor content
* Register /link custom slash command that opens CommandDialog in "link mode"
* On selection: inserts LinkChip with selected item's type and id
* Save chip data as custom BlockNote inline content JSON


Ticket 6-C: Backlinks panel


* In PageView.tsx: below editor, show "Linked from" section
* Query links table for all records where target_id = page.id
* Display as a list of linked item titles (clickable)
* On LinkChip insert: write to links table (source_id → target_id)


Acceptance criteria: ⌘K opens, searches, navigates. /link in BlockNote inserts chip. Backlinks panel shows references.


________________




Phase 7: AI Features (3–4 sessions)
Ticket 7-A: Tone system prompt and AI constants


* Create src/lib/aiConstants.ts
* Define TONE_SYSTEM_PROMPT: supportive, forward-focused language. Explicitly prohibits words: overdue, late, missed, behind, failed, incomplete, you should, you need to.
* Define AI_MODELS: { chat: 'claude-3-5-sonnet-20241022', suggestions: 'claude-3-haiku-20240307' }
* Every AI route imports from this file — never inline system prompts


Ticket 7-B: AI chat sidebar


* Install: npm install assistant-ui @ai-sdk/anthropic ai
* TanStack Start server function: src/server/aiChat.ts using streamText + toDataStreamResponse()
* AIChatPanel.tsx using assistant-ui Thread, ThreadMessages, Composer components
* useAIContext.ts: builds context string from current page/task/date for every message
* Panel toggles from top bar icon; on mobile renders as full bottom sheet
* Streaming renders word by word via useChat


Ticket 7-C: AI writing in BlockNote


* Install: npm install @blocknote/xl-ai
* TanStack Start server function: src/server/aiWriting.ts (same streamText pattern)
* Configure @blocknote/xl-ai to use the custom backend route (not direct Anthropic API key on client)
* AI toolbar button and /ai slash command are provided by @blocknote/xl-ai automatically
* Actions available: continue writing, summarize, rewrite, brainstorm below


Ticket 7-D: Scheduling suggestions


* src/server/schedulingSuggestions.ts server function
* Input: current tasks (urgent + important), current calendar blocks for next 7 days
* Output: array of { taskId, suggestedStart, suggestedEnd, reason } (using claude-3-haiku for cost)
* Display as non-blocking cards above the calendar bucket — "Consider scheduling: [task] on [day] at [time]"
* User can accept (creates block) or dismiss (hides for 24h)


Ticket 7-E: Journal AI prompts


* On journal page open: if page is new/empty, fetch an AI-generated prompt
* Server function: src/server/journalPrompt.ts
* Input: day of week, recent task completions, tone preference from profiles
* Output: single reflective prompt in a subtle banner above editor
* Banner has "Start writing" (focuses editor) and "Dismiss" buttons


Ticket 7-F: AI usage tracking


* useLogAIUsage(feature) hook: inserts to ai_usage on every AI call
* Features tracked: chat, writing, scheduling, journal_prompt
* In settings page: show "AI calls this month: [n]" as informational only — never as a warning or limit message


Acceptance criteria: AI chat streams. BlockNote AI toolbar works. Scheduling suggestions render. Journal prompt appears on new entries.


TEMPLATE SAVE (7-B): Extract streamText server function to C:\dev\coding_templates\ai\stream-route.ts TEMPLATE SAVE (7-A): Extract TONE_SYSTEM_PROMPT and aiConstants.ts structure to C:\dev\coding_templates\ai\tone-prompt.ts


________________




Phase 8: Polish, Mobile & Testing (2–3 sessions)
Ticket 8-A: Toast notifications (Sonner)


* npx shadcn@latest add sonner
* Add <Toaster /> to root layout
* Replace all current alert() / temporary error states with toast.success(), toast.error(), toast.warning()
* Add undo button to task complete: toast.success("Task complete", { action: { label: "Undo", onClick: () => undoComplete(taskId) } })
* Add undo button to task delete and block delete


Ticket 8-B: Empty states


* Use shadcnblocks.com empty state pattern for all empty views:
   * No tasks in bucket: "Your [Priority] list is clear. Add something when you're ready."
   * No pages: "Start a page — add it to a folder or leave it loose."
   * No table rows: "Nothing here yet. Add a row to get started."
   * New journal day: Journal prompt renders (from Ticket 7-E) — no generic empty state


Ticket 8-C: Mobile layout


* Test all views on 375px viewport
* Priority Bucket: renders as bottom sheet (shadcn Drawer) triggered by floating + button
* Calendar: initialView switches to timeGridDay on useMediaQuery('(pointer: coarse)')
* AI Chat: renders as full-screen bottom sheet on mobile
* Folder tree: collapses to modal sheet on mobile
* All tap targets minimum 44×44px


Ticket 8-D: Loading and error states


* Add shadcn Skeleton to all data loading states
* Add ErrorBoundary component wrapping each major view
* Handle Supabase .throwOnError() consistently with toast.error() in a global query error handler


Ticket 8-E: Unit tests


* Vitest tests for:
   * getJournalTitle() — all date range combinations
   * buildAIContext() — correct string for each route
   * BLOCK_SIZE_DURATIONS — correct minute values
   * buildSchedulingPayload() — correct task/block serialization


Acceptance criteria: All views show loading skeletons. Toasts appear on all user actions. Mobile layout functional. All Vitest tests pass.


TEMPLATE SAVE (8-B): Extract empty state component to C:\dev\coding_templates\components\empty-state.tsx


________________


Part 4 — Feature Checklist Against Product Spec
Every feature from the original product spec is mapped to a ticket:


Product Spec Feature
	Ticket
	Status
	Invite-only access
	1-A
	Planned
	Google OAuth login
	0-C
	Planned
	Priority Bucket (Urgent/Important/Someday)
	2-B
	Planned
	Task S/M/L block size
	2-B
	Planned
	Drag task from bucket to calendar
	3-D
	Planned
	3-day calendar view
	3-A
	Planned
	Morning/Afternoon/Evening zones
	3-B
	Planned
	Move + resize calendar blocks
	3-C
	Planned
	nowIndicator red line
	3-A
	Planned
	"Anytime" all-day slot
	3-A
	Planned
	Create blocks directly on calendar
	3-E
	Planned
	Google Calendar read sync
	3-F
	Planned
	Google Calendar write sync
	3-F
	Planned
	Folder tree with drag reorder
	4-A
	Planned
	Inline rename folders/pages
	4-A
	Planned
	Rich text block editor
	4-B
	Planned
	Autosave
	4-B
	Planned
	Journal section
	4-C
	Planned
	Journal auto-date title
	4-C
	Planned
	Custom tables with column types
	5-A/5-B/5-C
	Planned
	Inline cell editing
	5-C
	Planned
	Row detail page with notes
	5-D
	Planned
	⌘K global search
	6-A
	Planned
	Inline linking (/link command)
	6-B
	Planned
	Backlinks panel
	6-C
	Planned
	AI chat sidebar
	7-B
	Planned
	AI writing toolbar in editor
	7-C
	Planned
	/ai slash command in editor
	7-C
	Planned
	Scheduling suggestions
	7-D
	Planned
	Journal AI prompts
	7-E
	Planned
	Tone: supportive language only
	7-A
	Planned
	AI usage tracking
	7-F
	Planned
	Toast notifications + undo
	8-A
	Planned
	Dark mode
	1-B
	Planned
	Mobile layout
	8-C
	Planned
	Empty states
	8-B
	Planned
	Loading/error states
	8-D
	Planned
	User settings
	1-C
	Planned
	

________________


Part 5 — Coding Agent Workflow
Claude Code (terminal) — Planning, Ticketing, Review
Claude Code runs in the terminal and handles:


1. Reviewing each ticket before Cline starts coding
2. Breaking tickets into atomic sub-tasks if needed
3. Reviewing completed code for correctness, CLAUDE.md compliance, and tone
4. Updating memory-bank/activeContext.md and memory-bank/progress.md after each ticket


Session start command for Claude Code:


Read CLAUDE.md and memory-bank/activeContext.md.


Tell me the current ticket and what Cline needs to do to complete it.


Break it into the smallest possible sub-tasks.


Session end command for Claude Code:


Review the code Cline just wrote for Ticket [X-Y].


Check: CLAUDE.md rules, tone compliance, .throwOnError() usage, no Vite references, template save done.


Mark ticket complete in memory-bank/progress.md.


Set the next ticket in memory-bank/activeContext.md.


Cline + Qwen3 (VS Code) — Actual Coding
Cline handles all file creation and editing. At the start of every Cline session:


Read memory-bank/activeContext.md and memory-bank/techContext.md.


The current ticket is [X-Y].


Sub-tasks are: [paste from Claude Code output].


Follow all rules in CLAUDE.md.


Do not use Vite. Do not create files in api/. Use TanStack Start server functions only.


After completing all sub-tasks, run the dev server and confirm no console errors.


Avoiding Context Blowouts with Cline
* Keep each Cline session to one ticket maximum
* If a ticket has more than 5 sub-tasks, split it across two sessions
* After each session, run "update memory bank" in Cline to persist progress
* Never ask Cline to read the entire codebase — point it to specific files


________________


Part 6 — Environment Variables
Create C:\dev\second_brain\.env.local with:


# Supabase


VITE_SUPABASE_URL=https://[project-ref].supabase.co


VITE_SUPABASE_ANON_KEY=[anon-key]


SUPABASE_SERVICE_ROLE_KEY=[service-role-key]   # server functions only, never client


# Anthropic


ANTHROPIC_API_KEY=[key]


# Google OAuth (set in Supabase dashboard, not .env)


# Store google tokens in profiles table, not .env


# App


VITE_APP_URL=http://localhost:3000             # change to Vercel URL in production


ADMIN_EMAIL=[your-email]                       # for invite admin page access check


Save a copy of the variable names (no values) to C:\dev\coding_templates\env\.env.example.


________________


Part 7 — Deployment
Vercel setup:


* Connect GitHub repo to Vercel
* TanStack Start with Nitro adapter deploys automatically — no custom vercel.json needed
* Add all .env.local variables to Vercel Environment Variables dashboard
* Supabase: set VITE_APP_URL to Vercel production URL in Auth → URL Configuration


No separate serverless functions folder. No separate API deployment. All handled by TanStack Start + Nitro + Vercel.